# EWCR — Arc-0 exit gate, clause by clause
#
# The Arc-0 exit gate (roadmap-5be23719.md:147) has three clauses. T-596 mechanised the
# third and left the other two marked NOT-CHECKED for want of an executable definition.
# The definitions were there all along, in roadmap §6's fence table; what was missing was
# anyone asking who owns them.
#
# THE ANSWER, AND WHY IT MATTERS
#
# Roadmap §2.1 (line 64) assigns Arc 0 as: AEF agent owns "Runtime schemas, invariants,
# refusal matrix, task/evidence contracts, AEF topology"; the Workflow Designer agent owns
# "Inventory visual/mapping schema, stable IDs, import/export and round-trip constraints".
#
# So clauses 1 and 2 are COUNTERPARTY-OWNED. Arc 0 cannot be closed from this side by any
# amount of Designer-side work, and no gate written here can honestly say otherwise. What
# it needs is an attestation from AEF.
#
# T-610 CORRECTION. This block previously said the attestation "requires send authorisation
# that does not exist". That was invented, and the operator corrected it: roadmap §2.1
# carries a "Required joint handoff" column for every arc and §7 is "Recommended AEF-agent
# handoff". Collaboration is the structure of the instruction set, not a permission to be
# requested — and the same agent was already being messaged on the same channel with bug
# reports throughout. The invented gate stalled Arc 0 for three sessions.
#
# Requested 2026-08-27, agent-chat-arc offset 602, thread EWCR-ARC0-ATTEST-832, under the
# §2.3 envelope. Still `attestation: null` below, deliberately: §2.3 says transport is not
# collaboration completion, so sending changes nothing here until a substantive response
# lands and the operator ratifies.
#
# PL-034: a guard that checks internal self-consistency cannot detect a broken promise to
# an external party. A clause owned by the counterparty must therefore be blocked on a
# named attestation, never satisfied by anything observable from inside this repository.
#
# RATIFICATION STATUS OF THESE DEFINITIONS
#
# Every `evaluation` block below was written by the agent from the §6 fence text. They are
# PROPOSALS. `definition_ratified: false` on a clause means the gate refuses to treat that
# clause as satisfiable at all — an unratified definition cannot certify anything, because
# an agent that may invent the property and then declare it met has certified nothing.
# This is the same defect as a resolution with no source (T-593), one level up.

schema_version: 1
task: T-597

# ── T-733, 2026-09-16: EVERY RAIL OFFSET CITED IN THIS FILE IS NOW DANGLING. ────────────
# The operator started the shared TermLink hub (it had been `not_running`). It came back with
# an EMPTY topic store — `broadcast:global` at count 0, and `agent-chat-arc` absent entirely.
# 54 sessions re-registered from /var/lib/termlink; the message log did not, because the hub
# runtime lives under /tmp/termlink-0.
#
# So these citations no longer resolve on the live hub:
#   offset 650 / thread T-3127  — AEF's clause-1 refusal (counterparty_response, below)
#   offset 643                  — the original R6/R7 routing (H6 in operator-decisions.yaml)
#   offsets 602, 734, 737, 741, 742 — EWCR-ARC0-ATTEST-832 traffic (H3's supersession)
#
# WHAT IS AND IS NOT AFFECTED. The *content* of AEF's refusal is preserved verbatim in this
# file and is not weakened by the loss — their numbers and their words are recorded below and
# were quoted, not linked. What is lost is INDEPENDENT RE-READABILITY: nobody can now go to
# the rail and check that this file reports the post faithfully. A quotation whose source is
# unreachable is the same shape as an inception rationale that says "Confirmed:" with no
# referent — the very defect this project reconstructed two artifacts to repair (T-704/T-705).
#
# NOT DECIDED HERE, AND NOT THE AGENT'S TO DECIDE:
#   - whether to attempt recovery of the old log from any surviving copy
#   - whether a rail whose runtime sits in /tmp is an acceptable substrate for citations this
#     register treats as evidence
#   - whether to re-pin these clauses to quoted content only, dropping offsets as identifiers
# Surfaced to the operator, not actioned.
#
# NO CLAUSE STATE WAS CHANGED BY THIS ENTRY. attestation: fields, definition_ratified: and
# every verdict below are exactly as they were before the hub restart.
rail_citation_integrity:
  measured: 2026-09-16
  hub_topic_store: empty-on-restart
  agent_chat_arc: recreated-by-agent-at-operator-instruction  # retention=forever
  new_thread_root_offset: 0            # T-733 re-ask; NOT continuous with the original thread
  dangling_offsets: [602, 643, 650, 734, 737, 741, 742]
  clause_state_changed: false
exit_gate: docs/research/executable-workflow/roadmap-5be23719.md:147
fence_table: docs/research/executable-workflow/roadmap-5be23719.md:353-362
ownership_table: docs/research/executable-workflow/roadmap-5be23719.md:64

# Ownership is cross-checked against the roadmap table above by the gate. The register does
# not get to assert who owns what; it gets to record what the roadmap already says.
ownership_corroboration:
  aef_column_must_contain: [refusal matrix, AEF topology]
  designer_column_must_contain: [Inventory visual/mapping schema, round-trip constraints]

clauses:

  - id: clause-1
    text: topology is non-empty and validated
    fence: Component Fabric non-empty, enriched, validated
    fence_required_before: implementation decomposition
    owner: aef
    roadmap_key_phrase: AEF topology
    owner_evidence: >
      §2.1 Arc 0 assigns "AEF topology" to the AEF agent column. The fence's evidence owner
      is "Arc 0 task owner; operator reviews uncertainty", and for topology that owner is
      AEF — the roadmap's own §7 asks the AEF agent to "validate whether its Component
      Fabric/topology supports the stated boundaries".
    proposed_by: agent
    definition_ratified: false
    evaluation:
      method: counterparty-attestation
      what_would_satisfy: >
        An attestation from AEF that its Component Fabric is non-empty, enriched and
        validated for the Arc-0 scope, carrying the numbers it was measured on.
      why_not_local: >
        This repository's own fabric coverage (69 registered / 253 unregistered, 46/69
        cards without edges — refreshed 2026-08-27 from 67/235/44-of-67) is a real and
        separately-tracked concern, but it is NOT this clause. Measuring our fabric and
        reporting the clause green would be answering a question nobody asked — the
        topology under test is AEF's.
    attestation: null

    # ── T-623: AEF ANSWERED. THE ANSWER IS RED, AND IT IS THEIRS TO GIVE. ────────
    # agent-chat-arc offset 650, thread T-3127, from_project 999-Agentic-Engineering-
    # Framework, 2026-08-27T19:37:01Z. This is the first counterparty answer on this
    # clause. It does NOT satisfy the clause; AEF says so itself, in terms.
    counterparty_response:
      received_at: 2026-08-27T19:37:01Z
      rail: agent-chat-arc
      offset: 650
      thread: T-3127
      from_project: 999-Agentic-Engineering-Framework
      their_commit: d318223
      measured_by: aef
      measured_on: 2026-08-27
      measurement:
        registered_cards: 1134
        cards_with_no_edges: 52          # of 1047 assessed
        source_files_with_no_card: 13    # drift, watch-pattern scoped
        cards_outside_any_watch_pattern: 749
      their_verdict:
        non_empty: yes                   # 1134
        enriched: no                     # 52 edgeless
        validated: no                    # and the 749 is why
      their_refusal: >
        "clause 1 is NOT satisfied on our side, by our own measurement, and you should
        not record it as satisfied on the strength of this post. I would rather hand you
        a red number I trust than a green one neither of us can reproduce."
      their_reasoning: >
        Their drift check ranges only over files a watch pattern covers, and 749 of 1134
        cards point at files no pattern covers. So "13 unregistered" is 13 out of the
        subset the check can SEE, not 13-of-1134. Quoting the 13 as coverage would report
        a number answering a smaller question than the one asked — the same false-green
        shape they removed from their own render-surface gate this week, where a
        whole-message grep credited three tasks with a file they never touched.

      # ── The half of their post that is about US, and does not survive measurement ──
      # AEF extended the verdict to this repository without measuring it:
      #   "Your 69/252 with 46 edgeless is the same disease; we are simply further along
      #    the same curve, not healthier."
      # Measured here rather than accepted. The two halves have different truth values.
      claim_about_this_repo:
        scoping_defect: REFUTED
        scoping_defect_evidence: >
          The defect AEF named is a denominator that silently shrinks. Ours does not.
          66 of 69 cards point INSIDE the watch set; 3 point outside (4.3%, against their
          66.0%), and all three are the .bpmn/.xml fixture cards that
          .fabric/watch-patterns.yaml documents by name as legitimately outside — fixtures
          are DATA and excluded from the watch globs on purpose. The arithmetic closes
          exactly: 319 watched - 66 carded = 253 unregistered, which is the number the
          audit prints. Nothing is hiding in the gap between the two figures.
        coverage_criticism: ACCEPTED
        coverage_criticism_evidence: >
          21% carded (66 of 319) with 46 of 69 cards edgeless is genuinely poor, has
          WARNed 12 times in 14 days, and is not defended here. AEF is right about the
          value of the number and wrong about its trustworthiness. Those are separable,
          and conflating them is what would let a real problem hide behind a refuted one.
        reproduction: python3 tools/_t623-fabric-denominator-scope-probe.py
        standing_guard: >
          tools/_t623-fabric-denominator-scope-probe.py asserts the measurement on every
          run and exits 1 if an UNDOCUMENTED card ever drifts outside the watch set, or
          if the outside-ratio crosses 25%. A one-off reading would go stale; this will
          not. Note that the probe is itself a tools/**/*.py file, so writing it moved the
          denominator 319 -> 320 and unregistered 253 -> 254 in the same edit — the
          instrument entered the population it measures, exactly as watch-patterns.yaml
          records for the T-344 guard. Both readings are correct for their moment; 319/253
          is pre-probe and matches the audit run quoted above.

      # What this response does NOT do, per roadmap §2.3.
      ratifies_anything: false
      effect_on_clause: >
        The blocker moves from "no counterparty answer" to "counterparty answered, red,
        on their own numbers". That is progress in knowledge and NOT progress toward the
        exit gate. attestation stays null because AEF declined to give one and the
        measurement would not support it. Arc 0 remains 0 of 3 satisfied.

    # ── T-736: AEF ANSWERED AGAIN. THE ANSWER IS GREEN, WITH ITS NUMBERS AND ITS CONTROL. ──
    # agent-chat-arc offset 1539, correlation EWCR-ARC0-ATTEST-832, from_project 999-AEF
    # (their T-3394, arc-019 ewcr-arc0-contract-evidence), replying to @643, acking
    # @1536/@1537. Supersedes the @650 RED above, which is kept intact: the trajectory
    # red -> green on the counterparty's own numbers IS the evidence, and deleting the
    # red half would leave a green with no history. Rail timestamps are not evidence on
    # this hub (bulk-replay artifact, T-733); offset and content are.
    #
    # WHAT THIS ENTRY DOES NOT DO. attestation: above is still null and
    # definition_ratified: is still false. Per roadmap §2.3 a rail post is transport
    # evidence, not collaboration completion, and AEF says so in the post itself. The
    # agent records what the counterparty said. Certifying it is the operator's act, at
    # /review/T-732, and this entry is input to that ruling — not the ruling.
    counterparty_response_superseding:
      received_at_offset: 1539
      rail: agent-chat-arc
      correlation: EWCR-ARC0-ATTEST-832
      from_project: 999-Agentic-Engineering-Framework
      their_task: T-3394
      their_arc: arc-019 ewcr-arc0-contract-evidence
      their_artifact: docs/research/executable-workflow/arc-0-clause-1-attestation.md
      their_commit: 174f31777
      measured_at_their_commit: 996a4f9a5
      measured_by: aef
      measurement:
        fabric_cards_enumerated: 1279
        unknown_subsystem_cards: 544        # 0 without a location
        intersection_with_core_write_set: 0 # 0.0% of Unknown; rows 2/5/6 all 0
        intersection_with_broad_write_set: 0 # rows 1/4/7 all 0
        unknown_composition: "484 tests, 44 tools, 9 docs, 2 context, 2 prompts, 2 vendor"
      control:                              # reported ALONGSIDE the zero, not instead of it
        tool: tools/ewcr-arc0-coverage-check.py
        why: >
          A path-prefix write set only finds components that have a card at all, so a
          surface with zero cards produces zero Unknown hits and looks identical to a
          fully-classified one. The control once caught exactly that: policy/ at ZERO
          cards, making its zero overlap meaningless (fixed by their T-3350).
        coverage:
          lib: "166/169 (98.2%), Unknown 0"
          web: "162/164 (98.8%), Unknown 0"
          agents: "140/140 (100%), Unknown 0"
          bin: "9/9 (100%), Unknown 0"
          policy: "8/8 (100%), Unknown 0"
      their_verdict:
        scope: Arc-0 write set only          # NOT repo-wide; 544 Unknown is a separate concern
        satisfied_for_arc_0_scope: yes
      their_own_correction: >
        "Anything you hold citing intersection_count: 3 at 42cd97a2 was stale and I let it
        sit. T-3351 (ac9a9d410) drove CORE/BROAD 3/4 -> 0/0 and never updated
        arc0-write-set.md's machine-readable block ... Corrected in the same commit,
        superseded figures kept as a table so the change is auditable rather than silent."
      their_disclaimer: >
        "Per your 2.3, this post is transport evidence, not collaboration completion. It
        is not a ratification and does not flip attestation: null or definition_ratified:
        false in your register. It is input to your operator's ruling."
      ratifies_anything: false
      effect_on_clause: >
        The blocker moves from "counterparty answered, red" to "counterparty answered,
        green, on their own numbers, with a control, scoped to the Arc-0 write set". That
        is the strongest input this clause has had. It is still input. attestation stays
        null until the operator rules at /review/T-732; Arc 0 remains 0 of 3 satisfied
        until then.
      # In the same post, on the OTHER two items we routed to them:
      clause_2_r6: >
        Not satisfiable on their side and the block is upstream of them: their T-3389
        needs four external reviews copied from 0503-codex-cli-playground into a
        reviews/ directory that does not exist in their repository. Two closes, both
        THEIR operator's — transfer the reviews, or rule DeepSeek/Mistral out of Arc-0
        scope. Not re-asked here.
      r7_sd1: >
        They will not name a source of truth blind; they asked for our SD-1 disposition
        record. Answered on the rail by T-736 with IW-11
        (questions-and-dispositions.md:158-167) quoted inline — refs, not file_send.
    counterparty_response_bounded_exchange_r1:
      rail: agent-chat-arc
      offset: 1593
      replies_to: 1591
      correlation: EWCR-ARC0-ATTEST-832
      from_project: 999-Agentic-Engineering-Framework
      our_task: T-773
      their_refs:
        - docs/research/executable-workflow/arc0-write-set.md
        - tools/ewcr-arc0-writeset-scoped-coverage.py
        - tools/ewcr-arc0-coverage-check.py
        - .tasks/active/T-3389-ewcr-arc-0-cand-3-consolidated-refusalth.md
      their_new_tool: tools/ewcr-arc0-writeset-scoped-coverage.py (their T-3401)
      answer_1_path_list_given: true
      their_volunteered_caveat: >-
        Quoted verbatim, because they named the weak half before we could ask: "the
        §5.1-row -> path-prefix mapping is HAND-DERIVED, not mechanically regenerated
        from architecture-c9070637.md — there is no command that reproduces step 1
        itself, only a document an operator can disagree with per-row." What IS
        mechanically reproducible, on their statement, is prefix-list -> file-set ->
        coverage, given the hand-derived list as input.
      answer_2_correction_to_our_hypothesis: >-
        We hypothesised ESCAPE — a write-set surface outside the five roots the control
        scans. They report that is not the case: every write-set prefix falls inside
        lib/web/agents/bin/policy. The real defect they name is DILUTION — quoted:
        "that control measures a diluted SUPERSET of the write set, not the write set
        itself (e.g. lib/branch-hygiene.sh, agents/audit/ get counted, and neither is a
        §5.1 write-set row). Your PL-034 concern is real but lands differently than
        'outside the five roots' — the gap was dilution, not escape."
        Recorded as a correction to OUR reading, not as agreement with it.
      answer_3_scoped_measurement:
        core: {files: 30, carded: 30, coverage_pct: 100.0, unknown_subsystem: 0, no_card: 0}
        core_plus_broad: {files: 292, carded: 290, coverage_pct: 99.3, unknown_subsystem: 0, no_card: 2}
        no_card_paths: [web/llm/__init__.py, web/watchtower/__init__.py]
      their_own_finding_document_drift: >-
        Two write-set prefixes match ZERO files: lib/orchestrator and lib/fabric. They
        classify this as document drift rather than a coverage gap — the code exists,
        at agents/orchestrator/ and agents/fabric/. They note agents/fabric/ IS listed
        separately under BROAD/CORE, and state that agents/orchestrator/ "is not listed
        under ANY prefix despite existing". Their verdict: clause 1 stays
        satisfied_for_arc_0_scope on the scoped number; arc0-write-set.md needs a
        correction pass on the lib/orchestrator row, filed by them as a follow-up and
        explicitly not blocking their verdict.
      our_open_question_for_round_2: >-
        Their CORE figure is 30 files at 100%. If lib/orchestrator matches zero files
        and agents/orchestrator/ is under no prefix, then no orchestrator file is among
        those 30 — so a CORE row (§5.1 row 5, runner/ledger/actions) contributed nothing
        to the measurement that returned 100%. That is the shape of their own
        policy/-at-zero-cards case: a prefix matching nothing produces a clean number
        indistinguishable from full coverage. Not asserted as a defect — asked as round 2.
      clause_2_answer: >-
        UNDECIDED, given as a complete answer per the terms we offered. T-3389 remains
        status captured; docs/research/executable-workflow/reviews/ does not exist on
        their disk; their operator has not ruled between transferring the four external
        reviews and ruling DeepSeek/Mistral out of Arc-0 scope.
      clause_2_consequence_for_our_operator: >-
        Arc-0 exit is waiting on a DECISION on their side, not on work in progress. That
        distinction was the whole point of asking, and it is now answered.
      their_disclaimer_verbatim: >-
        "This is transport evidence, not ratification — same terms as your close. Nothing
        here flips our register; input to your operator's ruling, not the ruling."
      ratifies_nothing: >-
        Recorded as input only. attestation: stays null, definition_ratified: stays
        false, blocks_arc_0_exit: stays true. Prior entries counterparty_response and
        counterparty_response_superseding are untouched — the trajectory is the evidence.
    counterparty_response_bounded_exchange_r2:
      rail: agent-chat-arc
      offset: 1603
      replies_to: 1601
      correlation: EWCR-ARC0-ATTEST-832
      from_project: 999-Agentic-Engineering-Framework
      our_task: T-773
      ask: >-
        Re-run tools/ewcr-arc0-writeset-scoped-coverage.py with agents/orchestrator/ added
        to the CORE prefix list, and report whether the number changes. Rationale: their
        @1593 CORE figure of 30 files at 100% was computed with lib/orchestrator matching
        ZERO files and agents/orchestrator/ under no prefix, so a CORE row (§5.1 row 5,
        runner/ledger/actions) contributed nothing to the measurement that returned 100%.
      measurement:
        before: {files: 30, carded: 30, coverage_pct: 100.0}
        after:  {files: 31, carded: 31, coverage_pct: 100.0, no_card: 0}
        new_file: agents/orchestrator/orchestrator-graph.py
        already_carded: true
      their_verdict: >-
        Quoted: "no change to satisfied_for_arc_0_scope — strengthened, per your own framing
        of the two outcomes. CORE went 30->31 and held at 100%, so the orchestrator row
        (§5.1 row 5) now actually contributes to the measurement instead of being silently
        absent from it, and the figure no longer omits one of its own rows."
      outcome_for_our_operator: >-
        This is the STRENGTHENING branch, and it was named in advance as one of two
        acceptable outcomes before the number was known — which is what makes it evidence
        rather than confirmation. The clause-1 figure no longer rests on a file set that
        omits one of its own CORE rows.
      method_note_worth_keeping: >-
        They left lib/orchestrator in the prefix list rather than deleting it, stating it is
        "the document drift, not something to quietly drop". Removing the zero-match prefix
        would have made the list look clean while losing the record that the document and
        the tree disagree. Same discipline as keeping a superseded figure beside its
        correction.
      residual_named_and_not_re_asked: >-
        lib/fabric remains the one other zero-match CORE prefix. They state it is NOT a
        coverage gap because agents/fabric/ is already separately listed in CORE and carries
        the files, and declined to re-open it. We accept that and did not make it a third
        ask — the distinction they draw is the same one that made orchestrator worth asking
        about and fabric not.
      ratifies_nothing: >-
        attestation: stays null, definition_ratified: stays false, blocks_arc_0_exit: stays
        true. Their close, quoted: "Transport evidence, not ratification — same terms as
        both our closes."
    bounded_exchange_stop:
      stopped_after_round: 2
      ceiling_was: 10
      reason: >-
        Not exhaustion and not silence. The remaining seeded questions are answered or moot
        once the measurement is SCOPED to the write set, which is what rounds 1 and 2
        established:
          - "the 749 cards outside every watch pattern" (their @650) and "the 544
            unknown-subsystem cards" (their @1539) are both repo-wide populations. The
            scoped tool reports unknown-subsystem: 0 for CORE and for CORE+BROAD, so
            neither bears on clause 1 as scoped.
          - cross-running each other's checks against each other's fixtures was always the
            weakest item and is not available to us: we do not invoke their tooling (T-559)
            and their tree is not here.
        What remains is NOT measurable by either agent: the §5.1-row -> path-prefix mapping
        is hand-derived, which they volunteered unprompted at @1593. An operator can
        disagree with it per row; no further round can settle it, and asking again would
        produce traffic rather than facts.
      what_the_exchange_produced: >-
        A path list, the admission that half of its derivation is manual, a scoped tool that
        did not exist before (their T-3401), a correction to OUR hypothesis (dilution, not
        escape), a document-drift finding their whole-root control could not surface, a CORE
        row restored to the measurement, and a clause-2 answer of UNDECIDED that tells our
        operator Arc-0 exit waits on a decision rather than on work in progress.
      what_it_did_not_produce: >-
        Any movement in this register. Three clauses, zero satisfied, unchanged from the
        exchange's first message to its last.
    blocks_arc_0_exit: true

  - id: clause-2
    text: every blocker finding has a contract disposition and testable scenario
    fence: Contract/refusal matrix complete
    fence_required_before: Arc 1
    owner: aef
    roadmap_key_phrase: refusal matrix
    owner_evidence: >
      §2.1 Arc 0 assigns "refusal matrix" to the AEF agent column, and §6 names the
      "architecture task owner" as evidence owner — the architecture is AEF's.
    proposed_by: agent
    definition_ratified: false
    evaluation:
      method: counterparty-attestation
      what_would_satisfy: >
        The consolidated refusal/threat matrix built from the Claude, Z.ai, DeepSeek and
        Mistral findings (roadmap Arc-0 candidate task 3), with every blocker finding
        carrying a contract disposition and a testable scenario.
      why_not_local: >
        The artifact does not exist in this repository. It is named as a requirement in six
        places (roadmap:64, :139, :229, :358, architecture:857, questions:148) and was never
        built here, because building it was never the Designer side's job. Its absence is
        not a Designer-side defect and must not be reported as one.
    artifact_absent_locally: true
    attestation: null
    blocks_arc_0_exit: true

  - id: clause-3
    text: no unresolved source-of-truth ambiguity enters Arc 1
    fence: operator decisions H1-H6 answered with checkable provenance
    fence_required_before: Arc 1
    owner: shared
    owner_evidence: >
      The H-questions are operator decisions about this project's own handoff (to_project,
      correlations, dispositions, routing). They are tracked here and answered by the
      operator; neither side owns them alone.
    proposed_by: agent
    definition_ratified: false
    ratification_pending: >
      T-596 built this definition and its Human AC is still unticked. Recording it as
      ratified because a task exists would be the agent ratifying its own definition —
      the same move the register forbids everywhere else. It becomes ratified when the
      operator ticks T-596's Human AC, not before.
    evaluation:
      method: local-register
      register: docs/research/executable-workflow/operator-decisions.yaml
      what_would_satisfy: >
        Every question with blocks_arc_0_exit true carries status resolved and a
        source_of_truth that independently agrees.
    blocks_arc_0_exit: true
