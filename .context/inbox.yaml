# Observation Inbox - Unprocessed observations
# Capture: fw note "text"
# Review:  fw note list
# Triage:  fw note triage
observations:
- id: OBS-001
  text: "check-active-task.sh:92-107 — has_bash_write_pattern is tested BEFORE the T-2052 bootstrap exemption (elif), so a bootstrap verb carrying a write pattern (e.g. 'fw context focus T-X > log') is judged on focus state and blocked. MEASURED in T-381, not a wedge risk: all four remedies the block message actually prints (bare + 'cd && ' form, context focus + work-on) are ALLOWED. Defensible as-is (a redirect is a write, and writes need a task) — recorded so the ordering is a decision rather than an accident. Probe leg exists but is deliberately unscored: tools/_t381-focus-gate-wedge.sh B2."
  captured: 2026-08-08T15:22:21Z
  context_task: T-381
  tags: []
  status: pending
  promoted_to: null
- id: OBS-002
  text: "fw handover is BLOCKED by check-active-task when no task is focused - the command CLAUDE.md mandates at Session End Protocol step 4 cannot run at session end, because completing the last task nulls focus. Same family as T-381 (wedge in the wrap-up path). Workaround used: create a session-capture task (T-380/T-384 precedent), which is honest but means every session ends by creating a task whose only purpose is to permit the handover. Candidate fix: add 'fw handover' to the T-2054 no-active-task exemption alongside 'git commit' - CLAUDE.md already lists handover as allowed at critical budget, so the two policies disagree."
  captured: 2026-08-08T16:17:32Z
  context_task: T-384
  tags: []
  status: pending
  promoted_to: null
