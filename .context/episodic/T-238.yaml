# ============================================================================
# EPISODIC MEMORY - T-238: G-010: standing shell-runnable editor behavior suite (deep-link/autosave/claim legs)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-22T18:26:41Z
# ============================================================================

task_id: T-238
task_name: "G-010: standing shell-runnable editor behavior suite (deep-link/autosave/claim legs)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-22T18:17:35Z
completed: 2026-07-22T18:26:25Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - '`tools/_editor-behavior-verify-cdp.mjs` exists and runs green: exit 0, JSON verdict all legs pass:true (hermetic design — temp --repo + temp docroot sidecar, isolated chromium; structurally cannot touch real state)'
  - 'T-234 legs encoded and green: (1) jump-no-poison — ?load=arc-lifecycle → jumpToWorkflow(audit-process) → autosave record {id:audit-process, src:null} → revisit ?load ⇒ arc-lifecycle renders (14 nodes); (2) same-map edit-restore — mutation restored on reload via suppressed re-fetch path'
  - 'T-237 legs encoded via in-page parse→serialize, all green: (3) throw+eventDef stays intermediateThrowEvent (exactly 1 throw tag on re-export; payload-drop decision locked in); (4) catch+link+eventDef → linkEventCatch, workflowRef preserved; (5) bare catch → linkEventCatch; (6) typed catch → eventMessage + busTopic, aef:eventDef re-emitted'
  - 'READ-ONLY proven: 0 /api/save references in the suite source; real registry sha256 byte-identical across full runs (belt: the sidecar''s --repo is a temp dir anyway)'
  - 'Wired into tests/run-bridge-tests.sh (new section after corpus pins) via wrapper tests/test_editor_behavior.py with LOUD chromium/node SKIP (T-212 convention); full runner green, exit 0'
  - 'G-010 updated in concerns.yaml: status → prevention-in-place with T-238 + suite path + TEETH evidence cited; TEETH proven — vs pre-fix editor (git show 7390131^) the suite fails BOTH legs with the exact field symptoms (src poisoned to rendered/arc-lifecycle.bpmn, wrong map on revisit; throw→eventMessage mutation, 0 throw tags)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-07-22 — Hermetic sidecar over live-:8834 dependency'
    chose: 'the suite bootstraps its own throwaway gallery-serve.py (temp --repo, temp docroot with src/ editor + 2 rendered maps copied in) on a free port — the _typed-events-cdp.mjs architecture — instead of targeting the live :8834 server like _autoload-verify-cdp.mjs does.'
    rationale: '(1) read-only by construction — even a bug in the suite cannot mutate the real registry/claims/versions store (the T-235 claim is one-way; a stray save against :8834 could consume a fixture ghost); (2) runs in cron/CI contexts where :8834 may be down; (3) tests the SOURCE editor (src/), catching regressions before deploy.'
    alternatives_rejected: ['live-:8834 target — simpler but adds a server-up precondition and a real-state mutation risk; source-grep-only guard — G-010''s whole point is that greps don''t catch behavior.']
  - decision: '2026-07-22 — Teeth via pre-fix editor replay (argv editor override)'
    chose: 'the harness accepts an editor path override; teeth are proven by running it against `git show 7390131^:src/aef-workflow-designer.html` and requiring BOTH legs to fail with the field symptoms.'
    rationale: 'an in-run BITE mutation (typed-events style) can''t exercise cross-page persistence legs; replaying the actual pre-fix build proves the suite would have caught both shipped defects — the strongest possible non-vacuity evidence for a G-019 prevention claim.'
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-238-g-010-standing-shell-runnable-editor-beh.md
generated_by: context-agent-hybrid
