# ============================================================================
# EPISODIC MEMORY - T-775: create-task.sh emits unparseable frontmatter when a name or description contains a line break
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-21T14:36:15Z
# ============================================================================

task_id: T-775
task_name: "create-task.sh emits unparseable frontmatter when a name or description contains a line break"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-21T14:30:24Z
completed: 2026-09-21T14:35:50Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - '**The defect is reproduced against the shipped script, on both fields, before'
  - '**A line break in the name is refused, loudly, at the input boundary.** Guard added'
  - '**A multi-line description is CORRECTLY EMITTED, not refused.** Every line now'
  - '**The result parses.** Every leg feeds the emitted frontmatter to `yaml.safe_load`'
  - '**The T-774 bypass is closed and proven closed.** The attack was: put a break in'
  - '**Negative controls on every new leg, driven from the real pre-fix code.** The'
  - '**The structural blindness is registered as a gap, not just fixed.** **G-061**,'
  - '**The vendor divergence is declared.** Appended as `T-775`, `upstream: fix`, third'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-775-create-tasksh-emits-unparseable-frontmat.md
generated_by: context-agent-hybrid
