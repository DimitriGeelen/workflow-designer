# ============================================================================
# EPISODIC MEMORY - T-409: verification-hygiene baseline keys on relpath, so completing a grandfathered task fires a false red
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-09T10:58:02Z
# ============================================================================

task_id: T-409
task_name: "verification-hygiene baseline keys on relpath, so completing a grandfathered task fires a false red"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-09T10:54:48Z
completed: 2026-08-09T10:57:46Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-09T12:57:40+02:00
last_commit: 2026-08-09T12:57:40+02:00

# Summary (auto-generated from git commit messages)
summary: |
  key the hygiene baseline on task identity, not lifecycle location

# Key outcomes
outcomes:
  - 'The baseline is keyed on task-file **identity** (basename), not on `active/` vs `completed/` location; a grandfathered carrier that moves between the two still resolves to its baseline entry — `scan()` keys on `os.path.basename`, carrying `where` for reporting only'
  - 'A teeth leg proves the move case directly: grandfather a carrier in `active/`, move the file to `completed/` exactly as `work-completed` does, and require rc=0 with NO false red and NO spurious `RATCHET AVAILABLE` notice for that file — leg (g); the no-notice half matters because a spurious notice would invite `--tighten`, which would then drop a still-present carrier'
  - 'The ratchet still fires on a genuinely new carrier after the key change (teeth (a)/(b) still red) — narrowing the key must not widen the exemption — (a) (b) (b2) all still rc=1'
  - 'Two task files sharing a basename across `active/` and `completed/` cannot silently launder a carrier: the scan detects the collision and fails rather than treating one as the other — leg (g2), rc=2 COLLISION'
  - 'The existing baseline is migrated in place; `verification-hygiene.py` exits 0 on the real tree and `--census` still reads `serve-root-diff=75` / `hardcoded-port=17` (no existing verification line edited — leg 1 remains the operator''s)'
  - 'Full T-408 teeth suite still green end-to-end after the change — 12/12 legs (was 10/10; +g, +g2)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.tasks/active/T-409-verification-hygiene-baseline-keys-on-re.md'
  - '.tasks/completed/T-408-ratchet-the-g-015-verification-hygiene-p.md'
  - 'tools/_t408-hygiene-teeth.sh'
  - 'tools/verification-hygiene-baseline.json'
  - 'tools/verification-hygiene.py'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-09T12:57:40"
    action: "T-409: key the hygiene baseline on task identity, not lifecycle location"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 2 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 2
  commits: 1
  files_changed: 5
  lines_added: 430
  lines_removed: 104

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-409-verification-hygiene-baseline-keys-on-re.md
generated_by: context-agent-hybrid
