# ============================================================================
# EPISODIC MEMORY - T-829: Stop handing back blocked items: make each operator-only decision cheap, checked, or unnecessary
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-22T19:41:22Z
# ============================================================================

task_id: T-829
task_name: "Stop handing back blocked items: make each operator-only decision cheap, checked, or unnecessary"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-22T19:38:14Z
completed: 2026-09-22T19:41:15Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - 'Each of the six `[RUBBER-STAMP]` criteria is re-examined with the question **"what can I do to make this decision cheap, checked, or unnecessary?"** — NOT "whose is it?". Owner is the last question, not the first'
  - 'Where the blocker is a FACT held by the counterparty, it is obtained: AEF is asked on the rail. I have termlink and contacting AEF is mandated, not gated — classifying a question AEF can answer as "operator-only information" was my error, not a property of the item'
  - 'Any criterion that asks for something ALREADY DELIVERED is identified as stale, with the evidence and date — an AC that asks for what it already has costs the operator a re-read every time they triage'
  - 'Where a recorded answer exists, its independent corroboration is checked: does a guard actually enforce the register↔envelope agreement, or is that only claimed in prose?'
  - 'For every item that genuinely remains the operator''s, the work is reduced to a ONE-LINE answer with the evidence attached — so the cost is a decision, not a research session'
  - 'The pattern behind all of this is named as a learning: I have now created three tasks in one session with placeholder ACs and been caught by the gate each time. The reviewer''s "empty-body" FAIL was correct and my reading of it — that the queue was un-reviewable — was not'
  - 'Nothing human-owned is ticked, converted, or closed by me'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-829-stop-handing-back-blocked-items-make-eac.md
generated_by: context-agent-hybrid
