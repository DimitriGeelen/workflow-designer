# ============================================================================
# EPISODIC MEMORY - T-824: Cut designer 0.13.0 (operator-instructed release)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-22T18:21:38Z
# ============================================================================

task_id: T-824
task_name: "Cut designer 0.13.0 (operator-instructed release)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-09-22T18:17:11Z
completed: 2026-09-22T18:21:31Z
duration_days: 0
updates_count: 1
first_commit: 2026-09-22T20:18:27+02:00
last_commit: 2026-09-22T20:18:27+02:00

# Summary (auto-generated from git commit messages)
summary: |
  designer 0.13.0 — CUT and ANNOUNCED

# Key outcomes
outcomes:
  - '`VERSION` reads `0.13.0` and `APP_VERSION` in `src/` reads the same. The T-808 parity gate passes — it runs BEFORE any write to `dist/`, and a drifted version is refused rather than shipped'
  - '`scripts/release-designer.sh` runs to completion with NO bypass env var set: no `RELEASE_ALLOW_OVERWRITE`, no `RELEASE_SKIP_RENDER_CHECK`, no `RELEASE_SKIP_ANNOUNCE`. Each exists for a real situation; none of those situations is this one'
  - '`dist/aef-workflow-designer-0.13.0.html` is byte-identical to `src/aef-workflow-designer.html`, and `dist/MANIFEST.yaml` names it with a sha256 that matches the file on disk'
  - 'NO already-released artefact changed. `dist/aef-workflow-designer-0.12.0.html` and every earlier one are byte-identical to before — a version denotes fixed bytes or it denotes nothing (G-007)'
  - '`supersedes` in the manifest reads `0.12.0`, derived from what is actually present in `dist/`'
  - 'The release commit exists BEFORE the tag is applied. Tagging first would name a commit carrying neither this VERSION nor this artefact — a tag that looks right and resolves to the wrong tree'
  - 'Tag `designer-v0.13.0` exists, is annotated, names the sha256, resolves to the release commit, and is pushed to `origin`'
  - '`master` is fast-forwarded to carry the release, and the tag is an ancestor of `master` — a branch tip is not a release, and AEF fetch the artefact by tag (rail 483)'
  - 'The rail announcement either SUCCEEDED, or its failure is reported and the announcement is made through the MCP surface instead — a rail that still advertises 0.12.0 tells a consumer they are current when they are not, which is the false-green direction AEF called unacceptable'
  - '`python3 tools/_t382-release-lag.py` reports **BUILD LAG 0** — src carries nothing the release does not. ~~and the peer pin is in step~~ **THIS AC WAS WRONG AS WRITTEN and is corrected rather than ticked.** It demanded the gauge pass overall, but leg 2 measures ADOPTION lag — whether AEF has re-pinned — and they cannot have, seconds after the announce that tells them to. The gauge correctly reports WARN (exit 1), `peer pin behind: 0.12.0 -> 0.13.0, our release is 0 days old`. Demanding green here would have meant either faking a peer action or bypassing a gauge that is right. Same class as T-823''s leg 2: an assertion of a momentary alignment that the act it describes makes impossible'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-09-22 — release authority: instructed, and recorded as such'
  - decision: '2026-09-22 — no bypass, on a script that offers three'
    chose: 'run with `RELEASE_ALLOW_OVERWRITE`, `RELEASE_SKIP_RENDER_CHECK` and `RELEASE_SKIP_ANNOUNCE` all unset.'
    rationale: 'each exists for a real situation — a deliberate re-cut, a browser-less host, an unreachable hub — and none of those was this one. Reaching for a bypass because it is there is how a gate becomes decoration. The script''s own comment makes the point about the one flag it deliberately does NOT offer: there is no situation where shipping a knowingly mislabelled version is right, so there is no flag for it.'
  - decision: '2026-09-22 — immutability is now checked by something other than the releasing script'
    chose: 'pin every released artefact''s sha256 in `tools/_t824-dist-immutability.sha256` and verify it as a standing leg.'
    rationale: '`release-designer.sh`''s immutability guard fires only when IT is the thing doing the writing. It says nothing about a stray edit, a bad merge, or a well-meant reformat touching a shipped artefact — and that is exactly the case with no local signal, because AEF''s pin breaks on their side, not ours. I took the checksums BEFORE the cut and verified all 15 prior artefacts unchanged after; pinning them in the tree is what makes that a repeatable check rather than a thing I happened to do once.'

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'dist/aef-workflow-designer-0.13.0.html'
  - 'dist/MANIFEST.yaml'
  - 'src/aef-workflow-designer.html'
  - '.tasks/active/T-824-cut-designer-0130-operator-instructed-re.md'
  - 'VERSION'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-09-22T20:18:27"
    action: 'T-824: designer 0.13.0 — CUT and ANNOUNCED'

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 1
  files_changed: 5
  lines_added: 11714
  lines_removed: 10

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-824-cut-designer-0130-operator-instructed-re.md
generated_by: context-agent-hybrid
