# ============================================================================
# EPISODIC MEMORY - T-679: arc tag reassignment guard reads only arc_id:, so 26 legacy-tag-only tasks can be silently reassigned
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-05T10:53:36Z
# ============================================================================

task_id: T-679
task_name: "arc tag reassignment guard reads only arc_id:, so 26 legacy-tag-only tasks can be silently reassigned"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-05T10:51:14Z
completed: 2026-09-05T10:53:28Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - 'The reassignment guard consults the **same union the readers do** — a live `arc_id:`'
  - 'Tagging a legacy-tag-only task to the arc it **already** belongs to still writes'
  - 'A task carrying **two or more** `arc:` tags is refused with a message naming both'
  - 'The `arc:` tag scan is bounded to the `tags:` line inside frontmatter. `arc:` appears'
  - '`tools/_t467-arc-tag-source-of-truth.py` gains arms for each case above, and each new'
  - 'The 26 legacy-tag-only tasks are counted and reported, not migrated. Bulk-upgrading'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 2 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 2
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-679-arc-tag-reassignment-guard-reads-only-ar.md
generated_by: context-agent-hybrid
