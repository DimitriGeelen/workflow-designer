# ============================================================================
# EPISODIC MEMORY - T-462: G-037 second axis: agent-side recursive grep skips gitignored paths, and focus.yaml is one of them
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-12T17:01:36Z
# ============================================================================

task_id: T-462
task_name: "G-037 second axis: agent-side recursive grep skips gitignored paths, and focus.yaml is one of them"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-12T16:55:19Z
completed: 2026-08-12T17:01:32Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-12T19:01:26+02:00
last_commit: 2026-08-12T19:01:26+02:00

# Summary (auto-generated from git commit messages)
summary: |
  G-037 has a second face — the shim changes which FILES a recursive sweep reads, not just which patterns match. Peer-reported at rail 572, verified here rather than inherited: fixture sweep 0 agent-side vs 1 gate-side, bounded to recursion only (named file and glob agree 1==1 both ways). AEF's flag attribution corrected — --hidden REDUCES the divergence, --ignore-files causes it. The instance that matters on this tree is governance state: .context/working/focus.yaml and session.yaml are both gitignored, so a recursive sweep for the CURRENTLY FOCUSED task id finds 0 agent-side and 2 gate-side, and the 2 are the files recording that focus. Gauge now fails on either axis and names which — a green over half the class would be G-034's shape applied to this gap's own check

# Key outcomes
outcomes:
  - '**The second axis is MEASURED here, not inherited from the peer.** A peer''s finding'
  - '**Its scope is bounded, and the bound is the useful part.** The divergence is'
  - '**AEF''s flag attribution is corrected where it was imprecise.** They named'
  - '**The instance that matters on this tree is named, and it is governance state.** Only'
  - '**G-037 carries both axes and its gauge fails on either.** The register entry names'
  - '**The witness records the file-set fact, written from the shell that has the shim.**'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'G-037 has a second face — the shim changes which FILES a recursive sweep reads, not just which patterns match. Peer-reported at rail 572, verified here rather than inherited: fixture sweep 0 agent-side vs 1 gate-side, bounded to recursion only (named file and glob agree 1==1 both ways). AEF''s flag attribution corrected — --hidden REDUCES the divergence, --ignore-files causes it. The instance that matters on this tree is governance state: .context/working/focus.yaml and session.yaml are both gitignored, so a recursive sweep for the CURRENTLY FOCUSED task id finds 0 agent-side and 2 gate-side, and the 2 are the files recording that focus. Gauge now fails on either axis and names which — a green over half the class would be G-034''s shape applied to this gap''s own check'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.context/project/concerns.yaml'
  - '.context/working/.grep-witness'
  - '.tasks/active/T-462-g-037-second-axis-agent-side-recursive-g.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-12T19:01:26"
    action: "T-462: G-037 has a second face — the shim changes which FILES a recursive sweep reads, not just which patterns match. Peer-reported at rail 572, verified here rather than inherited: fixture sweep 0 agent-side vs 1 gate-side, bounded to recursion only (named file and glob agree 1==1 both ways). AEF's flag attribution corrected — --hidden REDUCES the divergence, --ignore-files causes it. The instance that matters on this tree is governance state: .context/working/focus.yaml and session.yaml are both gitignored, so a recursive sweep for the CURRENTLY FOCUSED task id finds 0 agent-side and 2 gate-side, and the 2 are the files recording that focus. Gauge now fails on either axis and names which — a green over half the class would be G-034's shape applied to this gap's own check"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 6 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 6
  commits: 1
  files_changed: 3
  lines_added: 320
  lines_removed: 39

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-462-g-037-second-axis-agent-side-recursive-g.md
generated_by: context-agent-hybrid
