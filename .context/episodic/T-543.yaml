# ============================================================================
# EPISODIC MEMORY - T-543: T-523 nesting teeth legs 7-8 fail intermittently inside the bridge suite but
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-16T18:01:58Z
# ============================================================================

task_id: T-543
task_name: "T-523 nesting teeth legs 7-8 fail intermittently inside the bridge suite but"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-08-16T14:26:20Z
completed: 2026-08-16T18:01:36Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-16T17:56:49+02:00
last_commit: 2026-08-16T18:06:45+02:00

# Summary (auto-generated from git commit messages)
summary: |
  the premise this task was filed on is dead, and killing it is the result. The description reasons 'legs 6 and 9 print out of order, so something is concurrent' and everything downstream of that — add a retry, raise the timeout — looked reasonable only because of it. tests/run-bridge-tests.sh contains ZERO backgrounding: no trailing &, no xargs -P, no wait, count 0. The suite is strictly serial, so no leg can interfere with another by running at the same time as it. Out-of-order lines have a duller explanation that costs nothing to believe: each leg's stdout is redirected to a file, so Python block-buffers stdout while stderr stays unbuffered and a process interleaves with ITSELF. Recorded as dead rather than quietly dropped, because a dead premise that is merely abandoned gets re-derived by the next reader. ARM A rules out the redirect as a CAUSE: 30 consecutive standalone runs with stdout redirected to a file, 30 passed 0 failed. One run is 7.25s, so the cheap discriminator was available all along and reaching for the 6-minute suite first would have been the expensive way to learn less. Arm B (3 consecutive full suites, per-leg outcomes) still running; rate AC and mechanism AC deliberately left UNTICKED because an unreproduced flake is not a fixed one and the bound has to be stated before either can be claimed. AC4 is itself a constraint made visible: no timeout loosened, no retry added, no leg quarantined until the mechanism is named — a suite that passes by waiting longer has stopped reporting, not stopped being wrong. CORRECTED IN PLACE, MY OWN ERROR AND EXACTLY THE CLASS UNDER STUDY: an earlier draft of the Findings section stated the first suite run 'took materially longer than the ~6 min baseline' and offered it as a datum. I had inferred elapsed time from how many steps I had taken while waiting rather than measuring it; ps showed 5:54 and a normal leg. Left in the file as a correction rather than deleted, because a task about a probe that certifies on nothing is the wrong place to quietly remove an impression dressed as a measurement. Also captured OBS-263 from the session survey: every termlink session on this host shares ONE identity_fingerprint (d1993c2c3ec44c94) — ours, AEF's five, email-archive's — so sender_id cannot distinguish which project wrote a message and attribution rests entirely on envelope.metadata, which termlink documents as unsigned. Our T-420 gate makes our own posts self-declare honestly but cannot authenticate an inbound post claiming to be AEF. Bears directly on the credential question raised today: a secret arriving over this rail could not be attributed to a sender even in principle. session hit budget warn with arm B incomplete — recording that as an unmet AC rather than salvaging a partial run. Run 1 of 3 was still executing, the job was launched detached to a session-scoped scratchpad path, and the next session is told to treat that file as absent and re-run from scratch. Partial output from an unattended run is precisely the evidence class this task exists to distrust; keeping it would repeat the error one level up. Rate AC and mechanism AC left unticked. mechanism established, and it is not flakiness — a probe sitting 4 seconds under a timeout, and a sweep that reports 'did not finish' as 'regressed'. Arm B run 1 reproduced the failure (104 passed, 1 failed, the failing leg being _t509-instrument-sweep.sh), and it then reproduced STANDALONE within minutes: rc=1, RAN 33 passed 32 failed 1, naming _t525-fabric-coverage-teeth.py (rc=124). 124 is timeout's exit code. Measured: _t525 takes 86.04s against the sweep's 90s cap and passes 7/7 cleanly on its own — 4.5% headroom. So it is not a suite-context failure at all; the suite is merely the most reliable way to load the machine, and the standalone reproduction happened while arm B was still running. My 'suite-context-only' framing was right in effect and wrong in mechanism. THE REAL DEFECT IS IN THE SWEEP'S REPORTING, NOT IN _t525: _t509 conflates 'the instrument failed' with 'the instrument did not finish', asserts 'an instrument that passed on 2026-08-15 no longer does', and states in its own explanatory text that 'a red here is a real regression in the thing it guards' — false for rc=124. Nothing regressed in fabric coverage; a probe ran out of wall-clock, and the reader is sent hunting a bug that does not exist. The sweep already knows how to honour abstention: it exempts _t364-t308-teeth.py BY NAME for exiting 2 'BY DESIGN, refusing to certify', reasoning that converting that to a suite failure 'would punish the honesty'. rc=124 is the same category of answer arriving through a different door and is not recognised — T-509's own shape a third time, an exemption granted to the case that prompted it and never generalised to the class. Deliberately NOT fixed: the tempting repair is raising the cap, and it is the wrong one, because _t525's cost tracks the size of the watched tree (251 watched, 198 unregistered today, growing with the repo) so the headroom would be consumed again. The repair that holds is teaching _t509 to report rc=124 as did-not-finish. AC4 stands — no timeout loosened, no retry added, no leg quarantined. _t523 IS NOT EXPLAINED BY THIS and the task says so: its symptom was drift_keys=[] measured node={} at rc=1, not rc=124, and it runs in 7.25s against a 600s ceiling — three orders of magnitude of headroom, so the timeout mechanism cannot account for it. Arm A's 30/30 leaves it unreproduced and its rate unbounded. Stated explicitly because the satisfying move is to declare the population solved on the strength of the half that was. Also withdrew my own over-cautious note from an hour ago telling the next session to treat arm B as absent: run 1 completed and emitted a full verdict line, so it is a complete observation. rate is 2 of 2, and run 2 is confounded by my own measurement apparatus. Run 1 gave 104 passed 1 failed, run 2 gave 100 passed 5 failed, both with the _t509 sweep among the failures; run 3 was still executing at session end. With n=2 that bounds very little and is explicitly not 'always', but it is no longer 'once, unreproduced', which is what the task was filed on. THE CONFOUND IS MINE AND IS RECORDED RATHER THAN AVERAGED IN: while run 2 executed I was running the sweep and _t525 standalone to identify the failing instrument, which put exactly the load the mechanism says pushes an 86s probe past a 90s cap. So run 2's four extra failures are evidence FOR the mechanism and simultaneously unusable as an independent rate sample. Left as a labelled row rather than a trend line, because 1 -> 5 reads as deterioration and part of it is the observer

# Key outcomes
outcomes:
  - 'The RATE is measured, not estimated: N consecutive full-suite runs with'
  - 'The claim this task was filed on — "legs 6 and 9 print out of order, so'
  - 'The mechanism is named with evidence, or the task records explicitly that'
  - 'No timeout is loosened, no retry is added, and no leg is quarantined'
  - 'The two instruments are treated as one population, not two incidents:'
  - 'If load introduced today is implicated, that is stated as our own doing:'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'mechanism established, and it is not flakiness — a probe sitting 4 seconds under a timeout, and a sweep that reports ''did not finish'' as ''regressed''. Arm B run 1 reproduced the failure (104 passed, 1 failed, the failing leg being _t509-instrument-sweep.sh), and it then reproduced STANDALONE within minutes: rc=1, RAN 33 passed 32 failed 1, naming _t525-fabric-coverage-teeth.py (rc=124). 124 is timeout''s exit code. Measured: _t525 takes 86.04s against the sweep''s 90s cap and passes 7/7 cleanly on its own — 4.5% headroom. So it is not a suite-context failure at all; the suite is merely the most reliable way to load the machine, and the standalone reproduction happened while arm B was still running. My ''suite-context-only'' framing was right in effect and wrong in mechanism. THE REAL DEFECT IS IN THE SWEEP''S REPORTING, NOT IN _t525: _t509 conflates ''the instrument failed'' with ''the instrument did not finish'', asserts ''an instrument that passed on 2026-08-15 no longer does'', and states in its own explanatory text that ''a red here is a real regression in the thing it guards'' — false for rc=124. Nothing regressed in fabric coverage; a probe ran out of wall-clock, and the reader is sent hunting a bug that does not exist. The sweep already knows how to honour abstention: it exempts _t364-t308-teeth.py BY NAME for exiting 2 ''BY DESIGN, refusing to certify'', reasoning that converting that to a suite failure ''would punish the honesty''. rc=124 is the same category of answer arriving through a different door and is not recognised — T-509''s own shape a third time, an exemption granted to the case that prompted it and never generalised to the class. Deliberately NOT fixed: the tempting repair is raising the cap, and it is the wrong one, because _t525''s cost tracks the size of the watched tree (251 watched, 198 unregistered today, growing with the repo) so the headroom would be consumed again. The repair that holds is teaching _t509 to report rc=124 as did-not-finish. AC4 stands — no timeout loosened, no retry added, no leg quarantined. _t523 IS NOT EXPLAINED BY THIS and the task says so: its symptom was drift_keys=[] measured node={} at rc=1, not rc=124, and it runs in 7.25s against a 600s ceiling — three orders of magnitude of headroom, so the timeout mechanism cannot account for it. Arm A''s 30/30 leaves it unreproduced and its rate unbounded. Stated explicitly because the satisfying move is to declare the population solved on the strength of the half that was. Also withdrew my own over-cautious note from an hour ago telling the next session to treat arm B as absent: run 1 completed and emitted a full verdict line, so it is a complete observation'
    source: git-mined
  - description: 'session hit budget warn with arm B incomplete — recording that as an unmet AC rather than salvaging a partial run. Run 1 of 3 was still executing, the job was launched detached to a session-scoped scratchpad path, and the next session is told to treat that file as absent and re-run from scratch. Partial output from an unattended run is precisely the evidence class this task exists to distrust; keeping it would repeat the error one level up. Rate AC and mechanism AC left unticked'
    source: git-mined
  - description: 'the premise this task was filed on is dead, and killing it is the result. The description reasons ''legs 6 and 9 print out of order, so something is concurrent'' and everything downstream of that — add a retry, raise the timeout — looked reasonable only because of it. tests/run-bridge-tests.sh contains ZERO backgrounding: no trailing &, no xargs -P, no wait, count 0. The suite is strictly serial, so no leg can interfere with another by running at the same time as it. Out-of-order lines have a duller explanation that costs nothing to believe: each leg''s stdout is redirected to a file, so Python block-buffers stdout while stderr stays unbuffered and a process interleaves with ITSELF. Recorded as dead rather than quietly dropped, because a dead premise that is merely abandoned gets re-derived by the next reader. ARM A rules out the redirect as a CAUSE: 30 consecutive standalone runs with stdout redirected to a file, 30 passed 0 failed. One run is 7.25s, so the cheap discriminator was available all along and reaching for the 6-minute suite first would have been the expensive way to learn less. Arm B (3 consecutive full suites, per-leg outcomes) still running; rate AC and mechanism AC deliberately left UNTICKED because an unreproduced flake is not a fixed one and the bound has to be stated before either can be claimed. AC4 is itself a constraint made visible: no timeout loosened, no retry added, no leg quarantined until the mechanism is named — a suite that passes by waiting longer has stopped reporting, not stopped being wrong. CORRECTED IN PLACE, MY OWN ERROR AND EXACTLY THE CLASS UNDER STUDY: an earlier draft of the Findings section stated the first suite run ''took materially longer than the ~6 min baseline'' and offered it as a datum. I had inferred elapsed time from how many steps I had taken while waiting rather than measuring it; ps showed 5:54 and a normal leg. Left in the file as a correction rather than deleted, because a task about a probe that certifies on nothing is the wrong place to quietly remove an impression dressed as a measurement. Also captured OBS-263 from the session survey: every termlink session on this host shares ONE identity_fingerprint (d1993c2c3ec44c94) — ours, AEF''s five, email-archive''s — so sender_id cannot distinguish which project wrote a message and attribution rests entirely on envelope.metadata, which termlink documents as unsigned. Our T-420 gate makes our own posts self-declare honestly but cannot authenticate an inbound post claiming to be AEF. Bears directly on the credential question raised today: a secret arriving over this rail could not be attributed to a sender even in principle'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.context/audits/2026-08-16.yaml'
  - '.context/audits/cron/2026-08-16-1745.yaml'
  - '.context/audits/cron/2026-08-16-1800.yaml'
  - '.context/audits/cron/LATEST-CRON.yaml'
  - '.context/audits/discoveries/LATEST.yaml'
  - '.context/bvp-driver-proposals.jsonl'
  - '.context/episodic/T-546.yaml'
  - '.context/inbox.yaml'
  - '.context/locks/audit.lock'
  - '.context/locks/T-543.lock'
  - '.context/locks/T-546.lock'
  - '.context/project/metrics-history.yaml'
  - '.context/working/.budget-gate-counter'
  - '.context/working/.budget-status'
  - '.context/working/.edit-counter'
  - '.context/working/episodic-gen/T-546.log'
  - '.context/working/feedback-stream.yaml'
  - '.context/working/.hook-counter'
  - '.context/working/.session-metrics.yaml'
  - '.context/working/.session-turn-offset'
  - '.context/working/watchtower.log'
  - '.tasks/active/T-358-importer-fabricates-lane-and-pool-struct.md'
  - '.tasks/active/T-543-t-523-nesting-teeth-legs-7-8-fail-interm.md'
  - '.tasks/completed/T-546-report-six-generic-upstream-defects-to-a.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-16T17:56:49"
    action: "T-543: the premise this task was filed on is dead, and killing it is the result. The description reasons 'legs 6 and 9 print out of order, so something is concurrent' and everything downstream of that — add a retry, raise the timeout — looked reasonable only because of it. tests/run-bridge-tests.sh contains ZERO backgrounding: no trailing &, no xargs -P, no wait, count 0. The suite is strictly serial, so no leg can interfere with another by running at the same time as it. Out-of-order lines have a duller explanation that costs nothing to believe: each leg's stdout is redirected to a file, so Python block-buffers stdout while stderr stays unbuffered and a process interleaves with ITSELF. Recorded as dead rather than quietly dropped, because a dead premise that is merely abandoned gets re-derived by the next reader. ARM A rules out the redirect as a CAUSE: 30 consecutive standalone runs with stdout redirected to a file, 30 passed 0 failed. One run is 7.25s, so the cheap discriminator was available all along and reaching for the 6-minute suite first would have been the expensive way to learn less. Arm B (3 consecutive full suites, per-leg outcomes) still running; rate AC and mechanism AC deliberately left UNTICKED because an unreproduced flake is not a fixed one and the bound has to be stated before either can be claimed. AC4 is itself a constraint made visible: no timeout loosened, no retry added, no leg quarantined until the mechanism is named — a suite that passes by waiting longer has stopped reporting, not stopped being wrong. CORRECTED IN PLACE, MY OWN ERROR AND EXACTLY THE CLASS UNDER STUDY: an earlier draft of the Findings section stated the first suite run 'took materially longer than the ~6 min baseline' and offered it as a datum. I had inferred elapsed time from how many steps I had taken while waiting rather than measuring it; ps showed 5:54 and a normal leg. Left in the file as a correction rather than deleted, because a task about a probe that certifies on nothing is the wrong place to quietly remove an impression dressed as a measurement. Also captured OBS-263 from the session survey: every termlink session on this host shares ONE identity_fingerprint (d1993c2c3ec44c94) — ours, AEF's five, email-archive's — so sender_id cannot distinguish which project wrote a message and attribution rests entirely on envelope.metadata, which termlink documents as unsigned. Our T-420 gate makes our own posts self-declare honestly but cannot authenticate an inbound post claiming to be AEF. Bears directly on the credential question raised today: a secret arriving over this rail could not be attributed to a sender even in principle"
  - time: "2026-08-16T17:57:22"
    action: "T-543: session hit budget warn with arm B incomplete — recording that as an unmet AC rather than salvaging a partial run. Run 1 of 3 was still executing, the job was launched detached to a session-scoped scratchpad path, and the next session is told to treat that file as absent and re-run from scratch. Partial output from an unattended run is precisely the evidence class this task exists to distrust; keeping it would repeat the error one level up. Rate AC and mechanism AC left unticked"
  - time: "2026-08-16T18:06:10"
    action: "T-543: mechanism established, and it is not flakiness — a probe sitting 4 seconds under a timeout, and a sweep that reports 'did not finish' as 'regressed'. Arm B run 1 reproduced the failure (104 passed, 1 failed, the failing leg being _t509-instrument-sweep.sh), and it then reproduced STANDALONE within minutes: rc=1, RAN 33 passed 32 failed 1, naming _t525-fabric-coverage-teeth.py (rc=124). 124 is timeout's exit code. Measured: _t525 takes 86.04s against the sweep's 90s cap and passes 7/7 cleanly on its own — 4.5% headroom. So it is not a suite-context failure at all; the suite is merely the most reliable way to load the machine, and the standalone reproduction happened while arm B was still running. My 'suite-context-only' framing was right in effect and wrong in mechanism. THE REAL DEFECT IS IN THE SWEEP'S REPORTING, NOT IN _t525: _t509 conflates 'the instrument failed' with 'the instrument did not finish', asserts 'an instrument that passed on 2026-08-15 no longer does', and states in its own explanatory text that 'a red here is a real regression in the thing it guards' — false for rc=124. Nothing regressed in fabric coverage; a probe ran out of wall-clock, and the reader is sent hunting a bug that does not exist. The sweep already knows how to honour abstention: it exempts _t364-t308-teeth.py BY NAME for exiting 2 'BY DESIGN, refusing to certify', reasoning that converting that to a suite failure 'would punish the honesty'. rc=124 is the same category of answer arriving through a different door and is not recognised — T-509's own shape a third time, an exemption granted to the case that prompted it and never generalised to the class. Deliberately NOT fixed: the tempting repair is raising the cap, and it is the wrong one, because _t525's cost tracks the size of the watched tree (251 watched, 198 unregistered today, growing with the repo) so the headroom would be consumed again. The repair that holds is teaching _t509 to report rc=124 as did-not-finish. AC4 stands — no timeout loosened, no retry added, no leg quarantined. _t523 IS NOT EXPLAINED BY THIS and the task says so: its symptom was drift_keys=[] measured node={} at rc=1, not rc=124, and it runs in 7.25s against a 600s ceiling — three orders of magnitude of headroom, so the timeout mechanism cannot account for it. Arm A's 30/30 leaves it unreproduced and its rate unbounded. Stated explicitly because the satisfying move is to declare the population solved on the strength of the half that was. Also withdrew my own over-cautious note from an hour ago telling the next session to treat arm B as absent: run 1 completed and emitted a full verdict line, so it is a complete observation"
  - time: "2026-08-16T18:06:45"
    action: "T-543: rate is 2 of 2, and run 2 is confounded by my own measurement apparatus. Run 1 gave 104 passed 1 failed, run 2 gave 100 passed 5 failed, both with the _t509 sweep among the failures; run 3 was still executing at session end. With n=2 that bounds very little and is explicitly not 'always', but it is no longer 'once, unreproduced', which is what the task was filed on. THE CONFOUND IS MINE AND IS RECORDED RATHER THAN AVERAGED IN: while run 2 executed I was running the sweep and _t525 standalone to identify the failing instrument, which put exactly the load the mechanism says pushes an 86s probe past a 90s cap. So run 2's four extra failures are evidence FOR the mechanism and simultaneously unusable as an independent rate sample. Left as a labelled row rather than a trend line, because 1 -> 5 reads as deterioration and part of it is the observer"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 4 commit(s), 215 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 215
  commits: 4
  files_changed: 24
  lines_added: 1290
  lines_removed: 332

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-543-t-523-nesting-teeth-legs-7-8-fail-interm.md
generated_by: context-agent-hybrid
