# ============================================================================
# EPISODIC MEMORY - T-322: IW-9 authority rules exist only on the XML form: the canonical YAML form has no sovereignty check (T-320 gap)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-01T10:24:51Z
# ============================================================================

task_id: T-322
task_name: "IW-9 authority rules exist only on the XML form: the canonical YAML form has no sovereignty check (T-320 gap)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-31T12:16:05Z
completed: 2026-08-01T10:23:27Z
duration_days: 1
updates_count: 2
first_commit: 2026-08-01T12:23:08+02:00
last_commit: 2026-08-01T12:23:08+02:00

# Summary (auto-generated from git commit messages)
summary: |
  IW-9 authority rules now decide on the canonical form too — and the census that found the gap was wrong twice

# Key outcomes
outcomes:
  - 'Carrier measured and recorded before implementation: **0 of 26** `.workflow.yaml` carry `workflowType` as a node key; the construct is nonetheless expressible — `workflowType` is in the canonical `aef:` vocabulary (`tools/yaml-to-bpmn.py` `META_KEYS`) and the designer offers it on `subProcess` (`src/aef-workflow-designer.html:1805`, options at `:1897`). Recorded in `## Evolution` with the corpus named, together with the census row it disproves (claimed 2/24 yaml; the 2 carriers are `.bpmn`).'
  - '`W-TYPE-LANE-MISMATCH` (O-1) emits from `Validator` at `tools/validate-workflow.py:_check_iw9_authority`, WARN, off the same module-scope `AUTHORITY_OWNER`/`TYPE_PERFORMER` tables the XML form now reads. Guarded on `authority is not None`, so absent authority stays silent. Fixture `tests/fixtures/warn/W-TYPE-LANE-MISMATCH.yaml` carries an in-file silence control (a `userTask` in the same sovereignty lane that must NOT warn).'
  - 'O-3 ported as `E-INCEPTION-NOT-SOVEREIGN` on `Validator` with PL-035 semantics — absent lane authority is a violation, not a skip. Proven by mutation M3: with the lane''s `authority` removed the rule fires `…its lane authority is absent (O-3, mapping-v1 §7)`.'
  - 'Teeth by mutation on the REAL tree (PL-061), three mutations, tree restored byte-identical (`git diff -- examples/` = 0 lines): **M1** real `userTask` in a sovereignty lane retyped `serviceTask` → `W-TYPE-LANE-MISMATCH` (pre-change build: `VALID — no findings`); **M2** real `subProcess` marked `workflowType: inception` in an `authority` lane → `E-INCEPTION-NOT-SOVEREIGN` (pre-change: `VALID — no findings`); **M3** same with the lane''s authority stripped → fires with `absent` (pre-change reported only `E-LANE-FIELD`, a different rule — verified, not assumed).'
  - 'Silence proven: 26 maps scanned, **0** findings for either new rule. Pinned as a Verification line.'
  - '`tests/test_rule_form_parity.py` updated — both entries moved to the new `PAIRED (same id, both forms)` classification, `EXPECTED_GAPS` 11 → 9 with the arithmetic re-derived in the comment. Guard green at `45 rules classified, 9 gaps`; no assertion was loosened to fit — one was **added** (see the third `## Evolution` entry: plain `PAIRED` did not survive its own teeth test).'
  - 'Pinned by fixtures in the GATING runner rather than a new orphan-prone module (`tests/run-validator-tests.sh` globs `fixtures/{invalid,warn}/*` by the `<RULE-ID>.<ext>` contract): 4 fixtures, both forms, the XML pair generated by bridging the YAML pair so the parity property is asserted directly. Validator suite 38 → **42 passed, 0 failed**; bridge **62 passed, 0 failed**.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'docs/reports/T-320-rule-form-parity-census.md'
  - '.tasks/active/T-322-iw-9-authority-rules-exist-only-on-the-x.md'
  - '.tasks/active/T-323-census-outofscope-classifications-rest-o.md'
  - 'tests/fixtures/invalid/E-INCEPTION-NOT-SOVEREIGN.xml'
  - 'tests/fixtures/invalid/E-INCEPTION-NOT-SOVEREIGN.yaml'
  - 'tests/fixtures/warn/W-TYPE-LANE-MISMATCH.xml'
  - 'tests/fixtures/warn/W-TYPE-LANE-MISMATCH.yaml'
  - 'tests/test_rule_form_parity.py'
  - 'tools/validate-workflow.py'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-01T12:23:08"
    action: "T-322: IW-9 authority rules now decide on the canonical form too — and the census that found the gap was wrong twice"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 1327 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 1327
  commits: 1
  files_changed: 9
  lines_added: 710
  lines_removed: 31

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-322-iw-9-authority-rules-exist-only-on-the-x.md
generated_by: context-agent-hybrid
