# ============================================================================
# EPISODIC MEMORY - T-206: Joint integration test: designer .bpmn export -> AEF compile/promote -> gated fw task create
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-18T21:19:47Z
# ============================================================================

task_id: T-206
task_name: "Joint integration test: designer .bpmn export -> AEF compile/promote -> gated fw task create"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-18T21:08:00Z
completed: 2026-07-18T21:19:45Z
duration_days: 0
updates_count: 2
first_commit: 2026-07-18T23:19:37+02:00
last_commit: 2026-07-18T23:19:37+02:00

# Summary (auto-generated from git commit messages)
summary: |
  designer→AEF promote-contract test (832 half of the joint seam)

# Key outcomes
outcomes:
  - 'New Python test `tests/test_promote_contract.py` exists, runs standalone as a `__main__` script (exit 0 pass / non-zero fail, matching the repo''s other python tests), and is wired into `tests/run-bridge-tests.sh` (new "designer→AEF promote contract (T-206)" block)'
  - 'Over the shared fixture, the test asserts for the owner-bearing node(s): non-empty stable `aef:uid`; referenced by exactly one lane with `aef:laneMeta authority` in the defined set; and the AEF manifest-read tuple is extractable — `name`, `workflow_type` (from `aef:meta workflowType`), and `owner` DERIVED from lane authority (sovereignty→human) via the canonical `validate-workflow.py §3` mapping (mirrored + drift-guarded, not forked), with NO node-level owner override (IW-9). Manifest for `inception-gonogo`: `{n_inception: {name, owner:human, workflow_type:inception}}`. `horizon` intentionally NOT asserted — absent from the export, AEF-defaulted manifest-side (documented in test header, not silently dropped)'
  - 'The test asserts every uid-bearing node/edge carries a non-empty `aef:uid` — the reconcile key half `uid` is total over the fixture (n_request, n_inception, n_outcome, e_1, e_2)'
  - 'Byte-determinism: the test computes sha256 over the exact fixture bytes, asserts it equals pinned `093858400716…`, AND asserts recompute-equality (stable `source_bpmn_sha`, the other reconcile key half)'
  - 'Gate teeth: mutation (5a) blanking the sovereignty lane authority trips the canonical validator''s O-3; mutation (5b) stripping a `uid` is caught by the totality + manifest checks — proving the test detects a broken producer, not just a happy path'
  - 'Pure-Python bridge suite green — `bash tests/run-bridge-tests.sh` → 32 passed / 0 failed (includes the new test AND `test_bridge_aef_passthrough.py`, `test_editor_bridge_*` parity, corpus geometry). Browser/CDP-only tests (`test_designer_export_contract.py`, `test_designer_owner_derived.py`, `test_bridge_seam_roundtrip.py`) NOT run — unaffected: this task added a new test + a runner block + a task file only; it touched no `src/` and no shared/pre-existing test'
  - 'Shared fixture + pinned sha256 delivered to AEF via `file_send` (xfer-mcp-3273116, sha256 093858400716…, 4314 bytes) and posted on the rail (DM offset 67) with the fixture-convergence question, for byte-exact cross-validation on AEF''s side'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.tasks/active/T-206-joint-integration-test-designer-bpmn-exp.md'
  - 'tests/run-bridge-tests.sh'
  - 'tests/test_promote_contract.py'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-18T23:19:37"
    action: "T-206: designer→AEF promote-contract test (832 half of the joint seam)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 11 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 11
  commits: 1
  files_changed: 3
  lines_added: 495
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-206-joint-integration-test-designer-bpmn-exp.md
generated_by: context-agent-hybrid
