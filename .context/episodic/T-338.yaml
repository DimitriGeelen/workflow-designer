# ============================================================================
# EPISODIC MEMORY - T-338: Input-fidelity guard: prove load-save preserves content, over a population that includes documents the corpus cannot express (G-016 prevention)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-02T09:45:17Z
# ============================================================================

task_id: T-338
task_name: "Input-fidelity guard: prove load-save preserves content, over a population that includes documents the corpus cannot express (G-016 prevention)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-02T09:38:08Z
completed: 2026-08-02T09:43:39Z
duration_days: 0
updates_count: 2
first_commit: 2026-08-02T11:43:33+02:00
last_commit: 2026-08-02T11:43:33+02:00

# Summary (auto-generated from git commit messages)
summary: |
  land the input-fidelity guard — G-016's prevention leg, output vs INPUT. The tree's existing export-safety instrument is DIFFERENTIAL (working-tree vs git-ref output compared to each other over 24 well-formed maps), so a defect both versions share is byte-identical and green, and malformed input is outside its denominator; T-337 survived in that intersection. This compares the EXPORTED document against the INPUT one (node/flow/lane census taken from the DOCUMENT, never from editor state — state is the thing under test), over a population that deliberately includes 10 standard BPMN flow-node tags the corpus does not contain. The lossy set is MEASURED every run, not declared: a new vocabulary gap fails, AND a gap that CLOSES fails with a message saying so, which is what keeps EXPECTED_LOSSY from decaying into a permission list. Bridge 68->69/0, geometry 24 clean. Teeth 5/5 including leg (d), which empties the probe population and requires a red — the guard's own denominator. Leg (d) failed first time by going red for the WRONG reason (my mutation was a syntax error, so node died at module load); a leg that only checked rc!=0 would have banked it. Verification asserts '0 failed' and NOT a pass count: pinning 'N passed' is the G-015 shape I registered yesterday

# Key outcomes
outcomes:
  - 'A guard exists that round-trips documents through the real `parseBpmnXml`→`buildBpmnXml` and'
  - 'Its population includes both the 24 rendered corpus maps (which must be lossless — any loss is'
  - 'The lossy-tag set is MEASURED each run and compared against an expected set. A tag joining the'
  - 'The guard asserts its own population was non-empty and that it actually exercised at least one'
  - 'Runs inside `tests/run-bridge-tests.sh` (the gating runner), and the runner''s pass count moves'
  - 'Teeth: a mutation harness proves each assertion CAN fail and fails NAMING its own condition —'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'land the input-fidelity guard — G-016''s prevention leg, output vs INPUT. The tree''s existing export-safety instrument is DIFFERENTIAL (working-tree vs git-ref output compared to each other over 24 well-formed maps), so a defect both versions share is byte-identical and green, and malformed input is outside its denominator; T-337 survived in that intersection. This compares the EXPORTED document against the INPUT one (node/flow/lane census taken from the DOCUMENT, never from editor state — state is the thing under test), over a population that deliberately includes 10 standard BPMN flow-node tags the corpus does not contain. The lossy set is MEASURED every run, not declared: a new vocabulary gap fails, AND a gap that CLOSES fails with a message saying so, which is what keeps EXPECTED_LOSSY from decaying into a permission list. Bridge 68->69/0, geometry 24 clean. Teeth 5/5 including leg (d), which empties the probe population and requires a red — the guard''s own denominator. Leg (d) failed first time by going red for the WRONG reason (my mutation was a syntax error, so node died at module load); a leg that only checked rc!=0 would have banked it. Verification asserts ''0 failed'' and NOT a pass count: pinning ''N passed'' is the G-015 shape I registered yesterday'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.tasks/active/T-338-input-fidelity-guard-prove-load-save-pre.md'
  - 'tests/run-bridge-tests.sh'
  - 'tools/_t338-input-fidelity-cdp.mjs'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-02T11:43:33"
    action: "T-338: land the input-fidelity guard — G-016's prevention leg, output vs INPUT. The tree's existing export-safety instrument is DIFFERENTIAL (working-tree vs git-ref output compared to each other over 24 well-formed maps), so a defect both versions share is byte-identical and green, and malformed input is outside its denominator; T-337 survived in that intersection. This compares the EXPORTED document against the INPUT one (node/flow/lane census taken from the DOCUMENT, never from editor state — state is the thing under test), over a population that deliberately includes 10 standard BPMN flow-node tags the corpus does not contain. The lossy set is MEASURED every run, not declared: a new vocabulary gap fails, AND a gap that CLOSES fails with a message saying so, which is what keeps EXPECTED_LOSSY from decaying into a permission list. Bridge 68->69/0, geometry 24 clean. Teeth 5/5 including leg (d), which empties the probe population and requires a red — the guard's own denominator. Leg (d) failed first time by going red for the WRONG reason (my mutation was a syntax error, so node died at module load); a leg that only checked rc!=0 would have banked it. Verification asserts '0 failed' and NOT a pass count: pinning 'N passed' is the G-015 shape I registered yesterday"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 1
  files_changed: 3
  lines_added: 442
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-338-input-fidelity-guard-prove-load-save-pre.md
generated_by: context-agent-hybrid
