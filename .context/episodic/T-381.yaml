# ============================================================================
# EPISODIC MEMORY - T-381: focus gate wedge: fw context focus accepts a completed task id its own reader rejects
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-08T15:25:44Z
# ============================================================================

task_id: T-381
task_name: "focus gate wedge: fw context focus accepts a completed task id its own reader rejects"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-08T15:17:56Z
completed: 2026-08-08T15:25:37Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - '**AC-1 — the wedge reproduces in isolation.** A sandbox project under the scratchpad'
  - '**AC-2 — the exit path is MEASURED, not assumed.** In the wedged sandbox, the hook is'
  - '**AC-3 — writer refuses a non-active id.** `do_focus` rejects an id that resolves only'
  - '**AC-4 — mechanism 2 resolved on the evidence.** If AC-2 shows the exemption is'
  - '**AC-5 — probe has teeth, proven by mutation.** `tools/_t381-focus-gate-wedge.sh`'
  - '**AC-6 — no bypass used.** No `--force`, `--skip-*`, `--no-verify`, `FW_SAFE_MODE=1`'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-08-08 — fix the writer, not the reader'
    chose: 'narrow `do_focus` to `find_task_file "$task_id" active`.'
    rationale: 'the reader''s requirement is the real constraint (G-013: a focused task must be'
    alternatives_rejected: ['widening the gate to accept completed tasks — that would let work proceed']
  - decision: '2026-08-08 — mechanism B: no code change, recorded as a disproved hypothesis'
    chose: 'leave check-active-task.sh:92-107 untouched.'
    rationale: 'AC-2 measured all four printed remedies as ALLOWED while wedged. The T-2052'
    alternatives_rejected: ['reordering the exemption above the write-pattern precheck on the strength of']
  - decision: '2026-08-08 — the probe measured the wrong tree under the gate''s environment'
    chose: 'strip `PROJECT_ROOT`/`TASKS_DIR`/`CONTEXT_DIR`/`FRAMEWORK_ROOT` from the env for'
    rationale: 'P-011 runs the probe from inside update-task.sh, which has already exported those'

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 7 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 7
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-381-focus-gate-wedge-writer-accepts-complete.md
generated_by: context-agent-hybrid
