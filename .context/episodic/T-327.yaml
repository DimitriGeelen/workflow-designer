# ============================================================================
# EPISODIC MEMORY - T-327: Gallery verify harnesses synthesise <bpmn:linkEventThrow>, a host tag no emitter produces (4 sites)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-01T21:44:46Z
# ============================================================================

task_id: T-327
task_name: "Gallery verify harnesses synthesise <bpmn:linkEventThrow>, a host tag no emitter produces (4 sites)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-01T21:24:20Z
completed: 2026-08-01T21:43:26Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - 'All 4 sites emit an emitter-producible host tag: `tools/_gallery-claim-verify.py`, `tools/_gallery-list-verify.py`, `tools/_gallery-registry-verify.py` (×2). Zero `<bpmn:linkEventThrow` remain in executable code (prose/comments explaining the history may remain and are excluded by comment-stripping, not by hand).'
  - 'All three harnesses still pass after the change. **This is the actual test of the finding:** if they pass on emitter-faithful bytes as readily as on malformed ones, they were never discriminating on the host tag — which is the claim. If any FAILS, that is a more interesting result and gets recorded, not patched around.'
  - 'The permitted element vocabulary used by the new guard is **derived from the emitters** (bridge `TYPE_MAP` / designer `TYPE_TAG`, via the existing declared-superset in `tests/test_xml_node_type_vocab.py`), never a hand-written list. A hand-written list drifts from the emitter silently and would go stale the moment a construct stops appearing.'
  - 'Guard scan surface is MEASURED and the measurement recorded in this task (which files, which distinct element names) before any assertion is written against it. An unmeasured scan that finds nothing is indistinguishable from a scan pointed at the wrong place — the exact defect AEF retracted at rail 367 (a capability gap reported against a directory that never existed).'
  - 'The guard strips comments before scanning. Negative-controlled: a `<bpmn:bogusTask` inside a COMMENT must NOT trip it, and the same literal in code MUST.'
  - 'Teeth: introducing a bogus BPMN element tag into a real harness makes the guard RED. Mutation landing asserted before the verdict is read (L-321); the guard''s finding count must actually move, not merely the tree (L-326). Tree restored, sha compared.'
  - 'Guard is wired into the GATING runner (`tests/run-bridge-tests.sh`), not merely present — an unrun guard cannot report a failure (L-316/orphaned-test-files).'
  - 'Gating suites green with counts recorded: bridge, validator, geometry.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 19 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 19
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/832-Workflow-designer/.tasks/completed/T-327-gallery-verify-harnesses-synthesise-bpmn.md
generated_by: context-agent-hybrid
