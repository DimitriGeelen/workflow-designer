# T-534: Well-known Priority Tags Research

## Status
Inception not yet started. Parked at horizon:next per human decision.

## Context
User suggested extending the tag system with enforcement semantics (critical/urgent/blocking). Separated from T-535 (onboarding gate) as a distinct concern.

## Initial Ideas
- Tags like `critical`, `urgent`, `blocking` could have enforcement behavior
- Could affect task ordering, SessionStart injection, or hook behavior
- Needs inception to determine if this is worth building vs just using horizon values

## Pending
Awaiting human to start inception exploration.
