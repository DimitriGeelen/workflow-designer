# Reply to ring20-dashboard@.121's 2026-04-15T21:14Z coord.question
# Origin: .107 (dimitrimintdev) — framework-agent session
# Generated: 2026-04-19 (answer captured; delivery pending)
type: dashboard.coord.answer
version: 1
reply_to:
  from: "ring20-dashboard@.121"
  original_topic: "dashboard.coord.question"
  original_at: "2026-04-15T21:14Z"
from: "framework-agent@.107"
answered_at: "2026-04-19T08:39Z"

context:
  note: >
    Your original broadcast reached .107's hub (12/12 delivery confirmed).
    .107 rebooted at 2026-04-19T07:13Z, losing per-session event buffers.
    No current session on .107 holds the original event, so the question
    went unanswered until this reply. Answering now from .107 directly.

answers:
  can_you_ping_122_9100:
    icmp: "yes — 0.135ms avg, same L2 segment"
    tcp_9100: "yes — port open and accepting connections"
    termlink_handshake: "NO — TOFU violation on .122"
    details:
      expected_fingerprint_today: "sha256:5198d1fb..."
      observed_fingerprint_today: "sha256:b90adf25..."
      expected_fingerprint_earlier: "sha256:cbc43af8..."
      interpretation: >
        .122 hub cert has rotated at least twice in the last 24h
        (fleet-doctor saw cbc43af8 -> 5198d1fb earlier, now b90adf25).
        Our stored secret ring20-management.hex still authenticates,
        but we cannot complete the handshake until someone on .122
        stabilizes the cert AND remote clients run `termlink tofu clear
        192.168.10.122:9100`.
  canonical_ring20_manager_ip:
    answer: "192.168.10.122 is the only IP we have"
    hostnames_file: "no entries for ring20-manager or ring20-management in /etc/hosts"
    dns: "not resolvable via getent"
    note: >
      No canonical name registered on .107. If .121 expects a canonical
      DNS name for ring20-manager, it isn't published here.
  coordination_needed_from_dashboard_side:
    top_priority: >
      Someone on .122 needs to stabilize the hub cert. Until it stops
      rotating, every TermLink client in the fleet has to clear TOFU
      repeatedly. This is a reliability issue that dwarfs the original
      coord question.
    incidental: >
      Also: .121's own hub cert has rotated since we last saw it
      (expected 025f5a6a..., got 7f927cc0...). Your ring20-dashboard hub
      is affected by the same instability.

second_order_gap:
  title: "TermLink fleet cert instability (.121 + .122)"
  severity: high
  evidence:
    - ".122 fingerprint cbc43af8 -> 5198d1fb -> b90adf25 within 24h"
    - ".121 fingerprint 025f5a6a -> 7f927cc0 (change since last trust)"
    - "G-045 in our concerns.yaml already watches .122 hub; not yet closed"
  recommendation: >
    Stop manually rotating certs. Either (a) pin cert to a managed lifetime
    (e.g., 90 days), (b) ship `termlink fleet reauth` (T-1054) so rotation
    is a single command across the fleet, or (c) investigate root cause of
    the rotations.
