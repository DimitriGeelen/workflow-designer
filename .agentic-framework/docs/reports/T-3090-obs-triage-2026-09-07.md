# Observation Triage — 2026-09-07 (T-3090)

Worker sweep over the 30 highest-numbered pending observations (OBS-334 … OBS-378).
No urgent-flagged pending observations exist in the inbox. Scope deliberately does
not overlap the earlier T-3289 w1–w4 sweeps (which covered OBS-201–317, 353, 355–366,
371–372). Analysis only — no inbox/concerns/task writes performed; parent integrates.

**Summary: 30 triaged — 19 PROMOTE, 5 MERGE, 3 LINK, 3 CLOSE, 0 KEEP.**

Verdict vocabulary: LINK = an existing task already covers this exact observation
(promotion already happened or title-matches verbatim); MERGE = duplicate/subset of an
existing task or OBS — attach as evidence, do not file anew.

| OBS-ID | Verdict | Target / evidence | Rationale |
|--------|---------|-------------------|-----------|
| OBS-334 | MERGE | T-3100 (active: "no error incident register — counter dead") | Obs explicitly "sharpens T-3100"; the broken skills-manager errors store is evidence for that aggregation inception, not a separate bug. |
| OBS-335 | PROMOTE | Task: "create_task.bats test 3 fails at HEAD and clobbers live focus.yaml — fix the sandbox escape" (build) | Pre-existing red test that escapes its own sandbox and trips the T-560 stale-focus gate on live sessions; no task covers it. |
| OBS-336 | PROMOTE | Task: "exec-bit drift detector — doctor/audit check comparing git index 100755 vs on-disk mode for *.sh" (build) | One-line detector already drafted in the obs; no `ls-files -s` mode check exists anywhere in bin/fw, audit.sh, or lib/. Whole audit rail was silently disabled once. |
| OBS-337 | PROMOTE | Task: "fw mirror status/sync must name the refs compared and cover master, not just the checked-out branch" (build) | "in sync" rendered green while github/master was 404 commits behind; no existing task (T-1843 is the stderr-hook sibling, different bug). |
| OBS-338 | PROMOTE | Task: "v1 BVP estimator assigns near-identical proposed scores — ranking degenerate at top-10" (build); link T-3068 | Cost half (82% unmeasurable) is already T-3068 (active); the estimator's per-task information collapse is the second, untracked cause. |
| OBS-339 | LINK | T-3116 (active: "redeploy resolver-loop.service with T-291x flags") | The obs itself filed T-3116 with real ACs; nothing further to promote. Held for human confirmation (live systemd infra). |
| OBS-340 | PROMOTE | Task: "tier0 status reports a 32h-old pending approval /approvals cannot see — expire or unify the two pending surfaces" (build) | `.tier0-approval.pending` still on disk (Aug 31); T-3080 is the adjacent-but-different TTL-grant mismatch (1h vs 5m), one-bug-one-task. |
| OBS-341 | PROMOTE | Task: "audit.bats asserts exit ≤1 — cannot distinguish audit-found-FAILs (2) from lock contention (75); assert !=75 and retry/skip" (test) | 9/14 unit failures in one live run were lock contention misreported as code failure; makes every "no new unit failures" AC non-deterministic. No covering task (T-3302 schedules the suite; doesn't fix the assertion). |
| OBS-342 | PROMOTE | Task: "doctor WARN when installed .git/hooks/* VERSION differs from agents/git/lib/hooks.sh source marker" (build) | T-3126's enforcing half was stale for ~2 days because nothing runs install-hooks; same source→installed drift class as L-364 cron chain; no doctor check exists. |
| OBS-343 | CLOSE | Fixed by T-3134 (completed: "P-011 verification extractor matches ## Ve…"); lib/verification-port.sh:202 now uses awk with first-range-only (`if (!seen)`) and `[[:space:]]*$` anchor | The unanchored repeating sed range the obs describes is gone; second "## Verification*" headings can no longer inject prose. |
| OBS-344 | LINK | T-3140 (active: "fw gaps renders only status:watching — 12 unresolved gaps invisible") | Task title is the observation verbatim; already promoted. |
| OBS-345 | PROMOTE | Task: "bats suite mutates live .context/working/focus.yaml — hermeticity lint: tests calling work-on/context focus must sandbox CONTEXT_DIR" (test) | Suite-wide class distinct from the single OBS-335 red test; sibling to L-606; no hermeticity task exists. |
| OBS-346 | PROMOTE | Task: "episodic re-mining fires on any T-NNNN in commit prose — scope hooks.sh:676 refresh to own-repo colon-prefixed task refs" (build) | Cross-project low-ID collision class (T-1376 family); forward risk is real with T-3145 ingestion naming peer task IDs; no covering task. |
| OBS-347 | PROMOTE | Task: "external writer mutated an active task's status/horizon/tags mid-session with no notice — attribute the sweep and surface task-file mutations by non-owning writers" (inception) | Attribution not established (envelopes ambiguous), so investigation before build; sovereignty concern regardless of which sweep did it. Origin task T-3146 is completed and did not cover this. |
| OBS-348 | MERGE | T-3297 (active: pre-push gate bounded lock wait `FW_PREPUSH_LOCK_WAIT` + contention-only Tier-2 bypass) | T-3297 (promoted from OBS-305) implements exactly the obs's two candidates (wait on lock; distinguish could-not-run from failed). Attach as evidence. |
| OBS-349 | MERGE | T-3282 (active; G-104 detector landed bd1ee1e3b: `fw watchtower current` verb, doctor WARN, audit + close-gate rule in CLAUDE.md §Web-touching tasks) | Obs's candidate fix (doctor WARN on stale Watchtower) is precisely what T-3282 shipped; merge as third measured instance. |
| OBS-350 | CLOSE | Content is the single word "status" — accidental capture, zero information | Nothing to act on; no evidence needed beyond the entry itself. |
| OBS-351 | PROMOTE | Task: "cron audit arrival rate exceeds service time — check schedule interval against p95 full-audit duration, back off or coalesce" (build); link T-3127, T-3297 | Distinct from T-3127 (timeout budget fit) and T-3297 (push-path resilience): this is queueing — instances stack and hold audit.lock continuously. Obs's own suggested pre-check (interval vs p95 from .context/audits/) is the AC seed. |
| OBS-352 | MERGE | T-2787 (active: "test fixtures escaped to filesystem root — guard against framework markers") | Same class, concrete new instance. Note for integrator: `/CLAUDE.md` (470B, fixture-consumer) is STILL on disk (Aug 26) and poisons every session on this host via the upward CLAUDE.md walk — cleanup is a one-line Tier-0-adjacent action worth doing at integration. |
| OBS-354 | PROMOTE | Task: "filing a task is indistinguishable from starting it — sanction a file-only route (commit a captured task's own file without the started-work round-trip)" (inception) | Governance/workflow-model question, not a bug; T-3299 covers sibling OBS-353 (gate blocks its own remedies) but not this filing-friction leg. |
| OBS-357 | CLOSE | Work correct and committed (ba4de211a); author deliberately chose not to rewrite history under 96% budget; residue is a lesson, not a task | Recommend integrator capture as a learning ("check `git status --short` for pre-existing dirt before staging by path — swept files commit under your task id"); nothing else actionable. |
| OBS-367 | MERGE | T-3301 (active: inception_decide_emit_review_post_move.bats red; body already names audit_inception_recommendation.bats) | Same emit_review code path; w4 triage explicitly directed the promoted task to diagnose both before splitting — one root cause suspected, diagnose-first. |
| OBS-368 | PROMOTE | Task: "doctor check 9 spends ~79s on bats --count and ignores --quick — guard with _doctor_quick_skip or use a cheap file count" (build) | Measured: `fw doctor --quick` blew a 120s timeout at check 8; doctor runs in hooks/cron/pre-push so cost is paid constantly. No covering task. |
| OBS-369 | PROMOTE | Task: "focus.yaml stomped twice in one hour by machinery replaying stale session S-2026-0813-2305 — find and kill the stale writer" (inception) | Fingerprint (Aug-13 session id + matching stale .budget-status) narrows to replay machinery, not a live session; w4 confirmed it is tracked nowhere; T-3030 is the broader live-checkout-workers umbrella but this specific writer needs attribution. |
| OBS-373 | LINK | T-3288 (active: "P-013 render-review gate blind to suffixed ### Human headings") | Already promoted from this obs; incident-site fix landed per OBS-375. |
| OBS-374 | PROMOTE | Task: "fw test passes --color=no when stdout is not a TTY; document ANSI-strip idiom next to L-387 capture-then-grep" (build) | False-RED blocked a close: pytest colorizes into redirected files so `grep -c '^FAILED'` counts 0. Small deterministic fix, no covering task. |
| OBS-375 | PROMOTE | Task: "centralize Human-heading extraction — audit.sh:6283 and lib/verify-acs.sh:294 still suffix-blind after T-3288's incident-site fix" (refactor) | L-317 cross-surface parity class; T-3288 fixed one parser and extracted lib/human_review_state.py — remaining divergent extractors need the consolidation pass. |
| OBS-376 | PROMOTE | Task: "which process auto-created T-3215 in started-work with template ACs, and should task-creation surfaces refuse that state" (inception) | Split out of OBS-353 by w3 triage precisely so it gets its own investigation; T-3299 covers the gate-wedge, not the creation path that produced the placeholder state G-020 exists to block. |
| OBS-377 | PROMOTE | Task: "codify the stale-verification class — P-011 lines and tests must anchor to fixtures/invariants, not mutable corpus state" (build) | Both named instances resolved themselves (T-2871, T-2969 now in completed/), but the class recurs (T-3300/OBS-359 is the same shape); deliverable is doc/lint guidance so closes stop being blocked by corpus drift. |
| OBS-378 | PROMOTE | Task: "audit section children inherit flock fd 200 — set FD_CLOEXEC/close before spawning so a SIGKILL'd audit's subtree can't extend the lock" (build) | Residual of the T-1464/T-1772 fd-inheritance class explicitly out of T-3298's no-unlink scope (T-3298 body confirms inode fix unaffected); observed live: exit-75 with no audit.sh process visible. |

## Integration notes for the parent

- **Highest-leverage promotes:** OBS-341 (unit-suite determinism — unblocks trust in every
  "no new failures" AC), OBS-368 (doctor latency paid on every hook/cron/pre-push run),
  OBS-336 (a mode bit silently disabled the entire audit rail once already).
- **Sovereignty pair:** OBS-347 + OBS-369 are both "something outside the session wrote
  session-owned state with no notice" — worth filing as siblings and cross-linking; if one
  investigation attributes the writer, it likely closes both.
- **Cleanup-at-integration:** OBS-352's leaked `/CLAUDE.md` is still live on this host and
  affects every Claude session started anywhere under `/`.
- CLOSE verdicts were held to citable evidence: OBS-343 (T-3134 + lib/verification-port.sh:202),
  OBS-350 (content-free entry), OBS-357 (committed work + deliberate no-rewrite; convert to learning).
