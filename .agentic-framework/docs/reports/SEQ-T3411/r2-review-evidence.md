# SEQ-T3411 Round 2 — Evidence file (Phase 3)

Gatherer/Judge NOT separated this round either (self-judged, single worker
`seq-t3411-r2-review`, one pass). Confidence capped one level below what
corroboration alone would justify, applied throughout §6 of `r2-review.md`.

Snapshot window: round 1's artifacts were written 2026-09-22T06:30–07:01Z.
This round's evidence was gathered 2026-09-22T07:02–09:10Z (~2h10m gap) —
short relative to the 14-day and 7-day windows some round-1 predictions
named; treat those as PARTIAL/IN-PROGRESS, not yet decidable, and say so.

| ID | Item | Source | Status of source | Data point (citation) | Window | Kind |
|---|---|---|---|---|---|---|
| E0 | Baseline version | `fw --version` | EXISTS | `fw v1.6.746`, HEAD `0ca99bcdb` on `bleeding-edge` (was `1.6.744`/`8bd9b13ec` at round 1) | now | structure |
| E1 | Origin sync state | `git status -sb`, `git rev-parse HEAD origin/bleeding-edge` | EXISTS | local `5e6ca3e…` is 4 commits **ahead** of `origin/bleeding-edge` (`8bd9b13e…`); unpushed: `0ec223c1e` (T-3412), `0ca99bcdb` (round-1 artifacts), plus any T-3414 commit | now | friction |
| E2 | Δ1 re-check: hook-write counters | `.context/working/.hook-counter` | EXISTS | 19 named hooks, all non-zero, range 2–3707 (e.g. `checkpoint=3707`, `check-active-task=3287`); no zero-count entries for the write-only class T-3371 fixed | since round 1, continuously written | usage |
| E3 | Δ1 re-check: latest full audit for hook-write-blindness citation | `.context/audits/2026-09-22.yaml` (fw audit, ts `06:32:21Z`, pre-dates T-3412 fix) | EXISTS | 33 PASS / 12 WARN / 0 FAIL; **zero** WARN/FAIL rows mention hook-write blindness or the hook counter | snapshot 06:32Z | usage |
| E4 | Δ2 re-check: nightly pytest leg | `.context/audits/unit-suite/LATEST.yaml`, `runs.log` | EXISTS, same defect | Latest run started `2026-09-22T01:03:01Z` (BEFORE round 1 began) finished `03:03:01Z`: `pytest: files=208 tests=0 failed_count=0 skipped=0 exit=124`. Next scheduled run is `2026-09-23T01:03Z` — no new data point exists inside this round's window | cron, daily 01:03Z | usage |
| E5 | Δ2: is this the same citation as round 1, or fresh? | comparison of E4 timestamp to round 1's E7 | — | **Same run** (round 1's E7 cited the identical `2026-09-22T01:03–03:03Z` run, six hours before this round). No new nightly cycle has occurred between round 1 and round 2 — the 14-day "Expected effect" window cannot be judged yet at n+1 rounds; only re-confirms the defect is still live | same snapshot as round 1 | usage |
| E6 | Δ3 re-check: gate-bypass-log parse | `python3 -c "import yaml; yaml.safe_load(...)"` on `.context/working/.gate-bypass-log.yaml`, run live this round | EXISTS, parses | `yaml.safe_load` succeeds, returns a list of **1211** entries (was failing to parse at all, 6+ weeks, per round 1 E5 and T-3412's handback) | now | usage |
| E7 | Δ3: what fixed it | `git log --oneline` | EXISTS | Commit `0ec223c1e` "T-3412: fix two independent gate-bypass-log.yaml corruption bugs" sits directly between round 1's two artifacts in the commit graph, timestamp-consistent with the procAsFit handback's own account | now | structure |
| E8 | Δ4 re-check: this round's own sidecar inbox | `fw sidecar inbox`, run 3× this round (session start, pre-write, pre-write-2) | EXISTS | `no pending consults on sidecar:seq-t3411-r2-review`, all 3 checks — **second consecutive round (r1, r2) with zero inbound consults on the review worker's own address** | this round | usage |
| E9 | Δ4: broader sidecar exercise beyond this round's own address | `.context/sidecar/outbox/*.json`, `awaiting-ack.jsonl`, `inbox-state.json` | EXISTS, PARTIAL evidence | 6 outbox messages + 6 awaiting-ack entries, all dated `2026-09-21T22:21–22:40Z` (before round 1 started at 06:30Z) — real hub-mediated sends (`state: INJECTED_NOW`, real deadlines), not unit-test fixtures. All 6 trace to `sidecar:999-Agentic-Engineering-Framework`, `sidecar:consult-responder`, `sidecar:ambient-responder` — the arc-011 sidecar's **own acceptance demo** for T-3407 ("ambient consults"), per commit `6506ecd32` and completed task `.tasks/completed/T-3407-arc-011-sidecar-slice-5-ambient-consults.md` | 2026-09-21T22:21–22:40Z (before this sequence) | usage |
| E10 | Δ4: independent (non-self) observer of delivery | `awaiting-ack.jsonl` content | PARTIAL | File only records `state: INJECTED_NOW` per message — no terminal ACK/FAIL state recorded in the file itself; this is still the sidecar's own bookkeeping (in-repo JSON), not a hub-side or third-party log. Ground Rule "a channel cannot report its own failures" still applies — this is corroborating usage evidence, not an out-of-band confirmation | same as E9 | usage / gap |
| E11 | Δ5 (T-3411 driver mechanism): TermLink topic `seq:T-3411` | `termlink channel snapshot seq:T-3411 --as-of <now> --json` | EXISTS | 5 posts, exactly matching the AC's contract: `round=1 step=review status=dispatched→complete`, `round=1 step=procasfit status=dispatched→complete`, `round=2 step=review status=dispatched` (this worker) — round/step/worker/result-path/status, one per transition, recoverable from the hub | 06:30:57Z–07:00:56Z | structure |
| E12 | Δ5: driver log corroboration | `.context/working/seq-t3411/driver.log` | EXISTS | Matches E11 exactly: dispatch → register → wait → OK(bytes) → next dispatch, for round 1 review (06:30:57–06:40:41Z, 18841 bytes) and round 1 procasfit (06:40:41–07:00:55Z, 13069 bytes); round 2 review dispatched 07:00:56–57Z | same | structure |
| E13 | Δ5: prompt files show round-fed-forward | `tools/prompt-sequence/r2-review.prompt.md` (28729 bytes, `mtime 09:00`, i.e. regenerated fresh for round 2, not stale from round 1's `06:30` copy) vs `r3-review.prompt.md` (28729 bytes but `mtime 07:42`, not yet regenerated) | EXISTS | The driver regenerates each round's prompt file at dispatch time (this round's file is newer than round 1's and newer than round 3's placeholder) — consistent with "each round fed the previous result" rather than a static template reused unchanged | now | structure |
| E14 | NEW: T-3414 — round 1's procAsFit fix shipped without a mandated Verification line | `.tasks/active/T-3414-*.md` | EXISTS | Task created `2026-09-22T07:02:42Z` (2 seconds after round 2's own dispatch): T-3412's `## Verification` block had **zero** occurrences of "vendor" despite editing 3 vendored paths (`lib/inception.sh`, `agents/context/check-active-task.sh`, `agents/task-create/create-task.sh`); CLAUDE.md §Vendored-path-touching tasks (OBS-250/T-3236) requires `bin/fw vendor self --check` in Verification BEFORE close. P-011 ran the 3 lines T-3412's author wrote and passed; the pre-push gate (T-2240/T-3125) caught what P-011 structurally could not, and named the fix | now | friction / structure |
| E15 | E14 cross-check: is T-3414 about this sequence? | Task body Context section | EXISTS | Explicit: "The SEQ-T3411 round-1 procAsFit worker fixed two real gate-bypass-log corruption bugs (T-3412, commit `0ec223c1e`) ... Pushing it was then refused by the pre-push self-vendor gate" | now | structure |
| E16 | E14: is the fix complete? | T-3414 AC checkboxes | PARTIAL | 3/4 Agent ACs ticked (vendor sync done, OBS recorded, T-3412's own fix untouched); **4th AC unticked**: "The push that was blocked now succeeds" — and E1 above confirms local HEAD is still 4 commits ahead of origin, so this AC's premise is still false as of this round's snapshot | now | friction |
| E17 | NEW: focus-state fallback hazard, observed live | `.context/working/focus.yaml` (shared) vs `.context/working/focus.seq-t3411-r1-procasfit.yaml` (per-worker, exists) vs `focus.seq-t3411-r2-review.yaml` (per-worker, **did not exist** until this round set it) | EXISTS (mechanism), OBSERVED (hazard) | `agents/context/check-active-task.sh:50-51`: "if session-local `focus.<key>.yaml` absent, fall back to shared `focus.yaml`, unchanged." At session start, no per-worker focus file existed for `seq-t3411-r2-review`; the shared `focus.yaml` pointed at `T-3414` (an unrelated, concurrently-active task on the same checkout). A read-only `find`/`ls`/`xargs` command was blocked by the G-020 gate citing **T-3414's** placeholder-AC state — not this worker's own task — until this worker ran `fw context focus T-3411` explicitly | this round, ~07:02–09:00Z | friction |
| E18 | E17: is this the G-083 hazard the task file names? | T-3411 task body, Context section (quoted in round 1's report) | EXISTS | Task file itself: "Sequential, one worker at a time: two autonomous writers on one tree is the live G-083 instance recorded earlier." E17 is a **milder, adjacent** instance: not two workers writing concurrently (T-3414 appears to be a separate, already-completed-by-the-time-I-checked concurrent session, not this sequence's own worker), but it demonstrates the *general* class — shared-tree workers inherit whatever focus state a concurrent session last set, and the gate's error message names the wrong task until the worker self-corrects | this round | friction |
| E19 | Δ6 re-check: CLAUDE.md:459 / `zzz-default.md` — is it the SAME file T-3370 flagged? | `CLAUDE.md:459`, `zzz-default.md` (repo root), `.tasks/templates/` | EXISTS (repo-root file) / ABSENT (templates-dir file) | `CLAUDE.md:459` (line number unchanged from T-3370's citation, file grew 1579 lines total, +?? since round 1): `` following `zzz-default.md` template ``. `.tasks/templates/` contains only `default.md`, `inception.md`, `path-c-deep-dive.md` — **no `zzz-default.md`**. A **different**, unrelated file named `zzz-default.md` (537 bytes, `mtime Feb 13 2026`) exists at repo root only | now | structure |
| E20 | Δ6: which template does task creation actually use? | `agents/task-create/create-task.sh:448,484-485` | EXISTS | `create-task.sh` resolves `TC_TEMPLATE="$TASKS_DIR/templates/default.md"` (or `inception.md` for inceptions) — **never** references `zzz-default.md` anywhere, root or templates dir | now | structure |
| E21 | Δ6 verdict basis | comparison of E19+E20 to round 1's Δ6 (E20 in r1) | — | Round 1's finding "zzz-default.md exists at repo root" was a **false-negative close**: it found a same-named but functionally unrelated file and treated the reference as possibly resolved. T-3370's original R18 citation (`.tasks/templates/zzz-default.md` — a path T-3370 stated explicitly, visible in `docs/reports/VALUE-REVIEW-whole-repo-2026-09-16-evidence-E-surfaces.md:98`) is **confirmed still accurate**: the templates directory has no such file, and the real template (`default.md`) is never named `zzz-default.md` anywhere the code reads it | now | structure |
| E22 | Baseline: `fw doctor` latency | `time timeout 90 bin/fw doctor` | EXISTS, unresolved | Timed out at 90s (exit 124), 66 lines of partial output produced before cutoff — same non-completion class round 1's E8 flagged at the 60s mark; this round confirms it at a longer timeout too, i.e. not a one-off slow run | now | friction |
| E23 | T-3302 (parked in round 1) — still parked? | `.tasks/active/T-3302-*.md` frontmatter, `.context/working/.reviewed-T-3302` | EXISTS, unchanged | `status: started-work`, `owner: human`, `date_finished:` empty — unchanged from round 1. `.reviewed-T-3302` marker file exists (`mtime 08:45`, i.e. touched by the gate the same run round 1's procAsFit triggered it), consistent with round 1's account that the review link was surfaced but no human action has landed yet | now | friction |
