# SEQ-T3411 Round 1 — Evidence file (GATHERER)

- **Role:** GATHERER. Facts only, no classification. Read-only against the live repo.
- **Snapshot time:** 2026-09-22T06:31:30Z (see row E0).
- **Scope:** whole repo (`/opt/999-Agentic-Engineering-Framework`).
- **Method note (stated up front, not buried):** this is not a from-scratch gather. A
  role-separated whole-repo value review (T-3370, dated 2026-09-16, 6 days old at
  snapshot time) already exists at `docs/reports/VALUE-REVIEW-whole-repo-2026-09-16*.md`
  — 11 files, ~3,400 lines, 6 GATHERER passes (A–F) + 2 JUDGE passes, 65 finding rows
  with zero blank cells. Per the ground rule "verify, don't reconstruct" and "activity,
  not calendar" (repo saw ~200 commits in the 6-day gap, most on an unrelated track —
  arc-011 sidecar, T-3395–T-3409), the higher-value gather for this round is: (1) spot-verify
  a representative set of T-3370's still-load-bearing claims against live state, (2)
  find out what has changed since, (3) inventory the one genuinely new subsystem that
  landed in the gap. Every row below is independently re-derived at snapshot time, not
  copied from the prior report — where it corroborates or contradicts T-3370, the row
  says so and cites both.

| ID | Item | Source | Status of source | Data point (citation) | Window | Kind |
|---|---|---|---|---|---|---|
| E0 | Snapshot time & repo identity | `date -u`; `git log --oneline -1`; `bin/fw --version` | EXISTS | `2026-09-22T06:31:30Z`; HEAD `8bd9b13ec` on `bleeding-edge`; `fw v1.6.744` | instant | structure |
| E1 | Repo scale | `find`, `ls` counts | EXISTS | `.tasks/active/*.md`=468; `.tasks/completed/*.md`=2930 (total 3398); `agents/`=26 dirs; `lib/*.sh`=96; `docs/reports/*.md`=727; `.context/arcs/*.yaml`=20 | instant | structure |
| E2 | Commit volume since prior review | `git log --since=2026-09-16 --oneline \| wc -l` | EXISTS | 200 commits in 6 days (2026-09-16 → 2026-09-22) | 6 days | usage |
| E3 | Prior review currency check — content | `git log --since=2026-09-16 --oneline \| grep -iE "T-3370\|T-3371\|T-3372\|sovereign"` | EXISTS | 14 matching commits found: T-3370 pass-1 (`316ee77b9`), operator ruling D-566 (`fa6c5241c`), pass-2 (`61804f65f`), I1 resolved (`dfe6604b5`), T-3371 hook-counter race fix (`128fd91d1`, `0c84b4497`, `bdc9f4532`), T-3372 witness restore (`b65d34d31`, `ba52cfd29`), one unrelated Sovereign-question filing on a different task (`e238a6ba9`, T-2918) | 6 days | usage/structure |
| E4 | T-3370 task file state | `.tasks/active/T-3370-*.md` frontmatter + body | EXISTS | `status: work-completed`, `owner: human`, Human AC `[ ] [REVIEW] Per-item approve/reject/modify` still **unticked**. `## Recommendation` present: GO on the review itself, Phase 6 not authorised, names 4 blocking rulings (yardstick, value-scoring fate, `--skip-sovereignty` authorisation, `.claude/settings.json` wiring approvals) | instant, dated 2026-09-16T15:56 | governance/friction |
| E5 | Gate-bypass-log parse status (T-3370 finding C §1.3 / A2) | `python3 -c "import yaml; yaml.safe_load(open('.context/working/.gate-bypass-log.yaml'))"` | EXISTS (file), FAILS (parse) | `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe2 in position 835: invalid continuation byte` — **identical failure mode** to T-3370's citation (byte 835, same codec error) | instant; file has 8,462 lines total, 1,224 top-level entries per `grep -c "^- "` | structure/friction |
| E6 | Hook-counter — write-only hooks (T-3370 finding I1) | `.context/working/.hook-counter` (raw `key=value` lines, not JSON — confirmed by direct read after an initial JSON-parse attempt failed) | EXISTS | Every hook I1 named now has a live non-zero count: `check-arc-id=536`, `check-onboarding-gate=536`, `check-human-ac-tick=536`, `check-inception-schema=536`, `check-heredoc-cmd-sub=535`, `check-inception-decisions=536`, `check-worktree-governance-write=536`, `check-settings-edit=527`, `commit-cadence=528`, `check-fabric-new-file=141`. T-3370 I1 (2026-09-16) had only a single-edit test (10/10) plus two counter snapshots that showed **zero** for this hook class. | instant, cumulative since last counter reset | usage — **corroborates T-3370's post-hoc claim that I1 resolved as "healthy" via the T-3371 race fix, with stronger evidence** (sustained counts, not one probe) |
| E7 | Unit suite status (T-3370 finding A1) | `.context/audits/unit-suite/LATEST.yaml` | EXISTS | `task: T-3302`, run window `2026-09-22T01:03–03:03Z`, `timed_out: true`, `runner_exit: 1`. bats leg: 398 tests, 13 failed, 10 skipped, `exit: 124` (timeout). pytest leg: `files: 208, tests: 0, exit: 124` — **zero pytest tests executed**, same defect class T-3370 A1 named ("pytest leg ran zero tests yet reported failed_count 0"); here `failed_count: 0` again despite 0 tests run and `exit: 124`. | run dated 2026-09-22, within the 8-day pattern T-3370 cited from a window ending 2026-09-16 | usage/friction — **unresolved, same defect, 6 days later** |
| E8 | fw doctor runtime | `timeout 60 bin/fw doctor` | EXISTS (command), TIMED OUT | Killed at 60s with no output captured — doctor did not complete within 60s on this host at this moment. Not a repeat of a specific T-3370 citation; new data point. | instant | friction (cost of running the primary diagnostic) |
| E9 | fw audit structural section | `.context/audits/2026-09-22-*.yaml` (latest, auto-run at 2026-09-21T23:02:44Z) | EXISTS | `pass: 33, warn: 13, fail: 0`. Notable WARNs: 4 stale arcs (`capability-overlay`, `dispatch-safety`, `horizon-axis-hardening`, `readme-first-run`, each >30d no task commit); **185 of 367 GO-recorded completed inceptions have unpropagated scope** (`related_tasks` empty, no back-reference, no `unlocks_inception_decision`); fabric `120/1209` cards with no edges, `774` cards pointing at files no watch pattern covers, `1` file with no fabric card; branch hygiene 18 findings; unit suite "COULD NOT DETERMINE" (ties to E7); continuous-run loop STOPPED (no-signal). | audit run dated 2026-09-21T23:02:44Z, ~7.5h before this snapshot | structure/friction |
| E10 | Arc status census (T-3370 Sovereign Q6: "17 in-progress arcs, none ever closed") | Per-file read of all 20 `.context/arcs/*.yaml`, `status:` field | EXISTS | `in-progress: 18`, `draft: 2`, `closed/abandoned: 0`. Total arc count grew 20→20 is flat vs T-3370's cited "17 in-progress" (net +1 in-progress over 6 days, still zero closures). | instant vs T-3370 baseline (2026-09-16) | value/governance |
| E11 | Partial-complete backlog (T-3370 Sovereign Q7: "265 active tasks carry date_finished") | `grep -l "^date_finished: 202" .tasks/active/*.md \| wc -l` | EXISTS | **278** active tasks now carry a `date_finished` (up from 265 cited in T-3370, +13 in 6 days) | instant vs 2026-09-16 baseline | value/friction — backlog is growing, not draining |
| E12 | Decision ledger since D-566 (the operator ruling T-3370 pass 2 applied) | `.context/project/decisions.yaml`, IDs D-567…D-583 | EXISTS | 17 new decisions recorded since D-566 (2026-09-16→2026-09-21). Topics: audit dead-negation lint, an unrelated arc-011 AC-gating pair (D-568/D-569), cron exec-bit gap (D-570/D-571), sidecar delivery/dedup semantics (D-572/D-574), two inception NO-GOs by supersession (D-575/D-577), a sandbox hardening trio (D-578/D-580), an AC-scoping edit (D-581), a test-fixture snapshot (D-582), a BVP facet-toggle scoping note (D-583). **None of the 17 answers any of T-3370 §12's 10 Sovereign questions** (yardstick confirmation, value-scoring fate, `--skip-sovereignty` authorisation, `.claude/settings.json` wiring approvals, Designer direction, arc closure, review backlog, govd/Antigravity retire-or-complete, `fw gpu`/`fw deploy` re-homing, instrumentation-cost approval). | 2026-09-16 → 2026-09-21 | governance |
| E13 | Instrumentation gaps G1/G2 (T-3370 §9) — still absent? | `find . -iname "*gate-eval*" -o -iname "*verb-count*"`; `grep -rl "gate_evaluation\|verb_invocation" lib/ bin/` | ABSENT (both) | Zero matches for either search. No gate-evaluation event log and no per-verb invocation counter exist anywhere in `lib/` or `bin/` at snapshot time. | instant | structure — confirms T-3370 G1/G2 still open |
| E14 | New subsystem since prior review: sidecar / peer-consult (arc-011, T-3395–T-3409) | `find` for `*sidecar*` across repo | EXISTS | Source: `lib/sidecar/__init__.py`, `outbox.py` (10 functions), `inbox.py` (9), `delivery.py` (7), `termlink_transport.py` (6); CLI `lib/sidecar_cli.py`; hook `agents/context/sidecar-inbox.sh`; vendored copies under `.agentic-framework/`. Fabric: 9 registered cards under `lib-sidecar-*`. Docs: 6 generated component pages under `docs/generated/components/`. Not present in, or evidenced by, any T-3370 datamap/evidence file (post-dates it by 6 days). | created 2026-09-21 to 2026-09-22 per git log (T-3397/T-3402/T-3404/T-3405/T-3406/T-3409) | structure |
| E15 | Sidecar test coverage and pass rate | `python3 -m pytest tests/unit/test_sidecar_outbox.py tests/unit/test_sidecar_delivery.py tests/unit/test_sidecar_termlink_transport.py tests/unit/test_sidecar_inbox.py -q` | EXISTS | `28 passed in 0.80s`, 0 failures, 0 skips, exit 0 | instant | usage/cost (test suite, not production traffic) |
| E16 | Sidecar CLI wiring | `bin/fw sidecar` (no args) | EXISTS | `usage: fw sidecar [-h] {whoami,send,inbox} ...` — three subcommands wired and reachable via `fw`, matching the CLAUDE.md-documented `fw sidecar inbox` verb this very run was instructed to use. | instant | structure |
| E17 | Sidecar real-world exercise (this run) | This session's own `fw sidecar inbox` call at task start | EXISTS (one data point) | `no pending consults on sidecar:seq-t3411-r1-review` — the mechanism responded correctly with an empty-inbox message using this worker's own registered identity. This is one successful round-trip (identity resolution + query), not evidence of cross-agent delivery (no consult was sent or received). | instant, this session | usage — thin: n=1, no send-side exercise |
| E18 | T-3411 driver mechanism itself | `.tasks/active/T-3411-*.md` body | EXISTS | This task IS the mechanism under partial test right now: a TermLink driver dispatching 5 rounds of (Review worker → procAsFit worker), each fed the prior round's result, posting run records to TermLink topic `seq:T-3411`. Per its own ACs, `--dry-run` support and the "refuse to advance on a missing result file" behaviour are asserted but not independently verified by this GATHERER (out of this round's scope — this round is itself the payload of step 1). | instant | structure — self-referential, flagged for INVESTIGATE rather than judged |
| E19 | Value-driver yardstick source (unchanged since T-3370) | `policy/value-drivers.yaml` | EXISTS | `version: 3`. Protected: D1 Antifragility (w9), D2 Reliability (w7), D3 Usability (w5), D4 Portability (w3). Free drivers active at T-3370 time: F-RECALL (w6), F3/F1 (w7 each, contested by T-3370 as internally contradictory — see below), F2, F-AUTONOMY (w4). File unchanged in substance since 2026-09-16 per this read; no new driver additions found in the `policy/prompts/` or decisions ledger scan (E12). | instant | value (yardstick input) |
| E20 | CLAUDE.md as governing document for this run | This session's system prompt | EXISTS | The project CLAUDE.md (loaded into every session) is itself an item this review's scope nominally covers ("docs" axis) but this run did not diff it against the prior review's R18 finding ("`CLAUDE.md:459` → nonexistent `zzz-default.md`"). Spot-check: `zzz-default.md` **does exist** at repo root per the Phase-0 `ls` (E1 raw listing) — so either R18's specific citation was already fixed, or the line number/claim needs re-verification against current CLAUDE.md content, which this GATHERER did not do (out of budget for this round). | instant | contradiction — flagged, not resolved |

## Non-use diagnosis rows (for items with low/no observed exercise)

| Item | A (broken) | B (never wired) | C (undiscoverable) | D (unmeasured) | E (not wanted) | Reading found |
|---|---|---|---|---|---|---|
| Sidecar peer-consult cross-agent delivery (E14/E17) | No error observed in the one call made | CLI is wired (E16), tests pass (E15), and this session's inbox call succeeded — not "never wired" | Documented in CLAUDE.md system-reminder for this exact run, so at least this caller found it | **Yes** — no send-side event was exercised or logged anywhere this GATHERER could find; a single empty-inbox read says nothing about whether consult delivery works end-to-end | No evidence either way | **D (unmeasured)** — the one exercise available (this run) only tested the read path with zero pending items |
| T-3411 5-round driver mechanism (E18) | Not observable from inside round 1 | N/A — task is `started-work`, actively running | N/A | **Yes** — this round cannot observe whether round 2–5 dispatch, feed-forward, or the `--dry-run`/missing-file-refusal ACs hold | No | **D (unmeasured)**, self-referential — round 1 is itself the first unit of evidence and cannot judge the whole |
| Gate-bypass-log parse (E5) | **Yes** — same UnicodeDecodeError as 6 days ago, unrepaired | N/A (it exists, it's just unreadable) | N/A | N/A | No — T-3370 A2 named repair as wanted; no operator ruling rejected it | **A (broken)**, confirmed persisting |
| Unit-suite pytest leg (E7) | **Yes** — 0 tests collected/run yet `exit: 124` and `failed_count: 0`, same shape T-3370 A1 flagged | N/A | N/A | N/A | No | **A (broken)**, confirmed persisting, 6 days later |
| G1/G2 instrumentation (E13) | N/A | **Yes** — repeatedly requested (T-3370 G1/G2, ranked #1/#2 unlock value) but no code exists | N/A | N/A | No — nothing rescinded the request | **B (never wired)**, confirmed persisting |

## What this GATHERER did NOT re-derive (inherited from T-3370, not re-verified this round)

Per "activity, not calendar": the following T-3370 evidence areas saw no material git activity in the 6-day gap (checked via `git log --since=2026-09-16 -- <path>` returning 0 or unrelated hits) and were not re-walked:
`tools/`, `scripts/`, `deploy/`, `install.sh`, `vendor/`, `.github/`, `web/` blueprint/recall-engine logic, security posture (`FW_SECRET_KEY`, Werkzeug warnings), the T-2621 conformance rail internals, per-verb churn, test-suite content quality beyond E7, git-hooks contents. All of T-3370's REFACTOR (R1–R22), the bulk of its ADD (A1–A25 except A2/A1 re-verified above), and 13 of its 16 INVESTIGATE rows are carried forward unchanged — see the JUDGE report (r1-review.md) §carry-forward.

## Commands run (disclosure)

`fw sidecar inbox` (once, at session start, per driver instruction) · `bin/fw --version` · `git log` (read-only, several invocations) · `find`/`ls`/`grep`/`wc` (read-only) · `timeout 60 bin/fw doctor` (timed out, no mutation) · `timeout 30 bin/fw metrics` · `python3 -c "yaml.safe_load(...)"` ×2 · `python3 -m pytest` on 4 sidecar test files (read-only test run, no fixtures mutated outside pytest's own tmp dirs) · `bin/fw sidecar` (no-arg, prints usage) · `mkdir -p docs/reports/SEQ-T3411`. No `Write`/`Edit` to any governed file occurred before this evidence file itself. No task-state mutation, no gate bypass invoked.
