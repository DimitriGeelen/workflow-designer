# VALUE REVIEW — Round 4 evidence file (T-3411, seq:T-3411)

Facts only, no classification. Cited by `r4-review.md` §6.

| ID | Source | Status | Data point | Window | Kind |
|---|---|---|---|---|---|
| E0 | `date -u` | measured | Session start: `2026-09-22T08:18:05Z` (driver log shows this worker spawned `08:17:37Z`, registered `08:17:35Z`) | instant | structure |
| E1 | `.context/working/focus.yaml` read BEFORE this worker called `fw context focus` | measured, direct read | `current_task: T-3421` (a real, unrelated, concurrently-active task — the very task diagnosing Δ7's root cause, see E9-E12) — a 4th distinct foreign task across this sequence's fresh-worker spawns (round 2 review saw `T-3414`, round 3 review saw `T-3418`, round 3 procasfit's own session-start observation saw `T-3420`) | instant, pre-mutation | friction |
| E2 | `ls .context/working/focus.seq-t3411-r4-review.yaml` (after `fw context focus T-3411`) | measured | Session-local focus file created correctly for this worker; the shared `focus.yaml` remained pointed at `T-3421` afterward (unmodified — correctly isolated, matches round 1-3's own pattern) | instant | structure |
| E3 | `git status -sb` | measured | `bleeding-edge...origin/bleeding-edge [ahead 5]` — up from round 3's end-state (`ahead 1`); 4 more commits landed without a successful push since round 3's own baseline | live | structure |
| E4 | `git log origin/bleeding-edge..HEAD --oneline` | measured | 5 unpushed commits: `c09603081` (round 3 procasfit handback), `9d121ed8f` (T-3417 close), `14b076e7d` (T-3414 close), `0943bfef0` (T-3420, not closed — see E13-E15), `47d573ad9` (round 3 review) | live | structure |
| E5 | `ps aux \| grep audit.sh` | measured, live | 3 concurrent `agents/audit/audit.sh --section structure` processes running at the instant of this check (PIDs 3855129, 3855162, 3888709) — direct, first-hand confirmation that the lock contention T-3421/round 3 diagnosed is actively occurring right now, not merely historical | instant | structure |
| E6 | `find .tasks -iname "*T-3421*"` + AC/status grep | measured | `status: started-work`, `owner: agent`. All 5 Agent ACs unchecked (`- [ ]`) — task is genuinely in progress, not done | live | structure |
| E7 | `git diff agents/git/lib/hooks.sh` (uncommitted, another session's working tree) | measured, read-only | T-3421's fix is real and matches its own AC text: derives `FW_PREPUSH_LOCK_WAIT` default from `.context/audits/full-audit-timing.yaml`'s last measured `structure` duration (1.25×, clamped [90,600], 360 fallback) instead of the fixed 90s default round 3 root-caused. Block message updated to name the new source and a 600s (was 300s) manual override suggestion | instant | structure |
| E8 | `ls -la lib/prepush-lock-wait.sh tests/unit/t3421_prepush_lock_wait.bats` | measured | Both files exist, untracked, 2765 and 2786 bytes respectively, mtimes `10:16`/`10:17` — created minutes before this check | instant | structure |
| E9 | `cat .context/audits/full-audit-timing.yaml` | measured | `structure: 292` (seconds), from the `2026-09-05` timed-out full-audit run — the exact ledger entry T-3421's own AC text cites ("ledger with structure=292 → 365"); 292×1.25=365, confirming the fix's formula is wired to the real recorded value, not an invented one | live (ledger dated 2026-09-05, unrelated to this sequence) | structure |
| E10 | Not exercised — T-3421's bats test / lib function were NOT run by this worker | n/a | Per Ground Rules ("exercising an item... never anything with external side effects" + one-lock-at-a-time / converging-writes discipline), this worker did not execute another session's uncommitted, unowned, in-flight code. Read-only `diff`/`ls`/`cat` only | n/a | (declined) |
| E11 | `git status -sb` untracked-file scan (`?? 6`, `?? "FABRIC: 2 component(s) modified: v1Send, outboundSendLog"`, `?? Summary:`) | measured | Three 0-153 byte files at repo root, all mtime `2026-09-05 20:57`, unrelated to any SEQ-T3411 activity, no reference to them in rounds 1-3's reports (`grep -c "stray\|'./6'\|FABRIC: 2 component" docs/reports/SEQ-T3411/r*.md` → 0 hits everywhere) | live, present ≥17 days | structure |
| E12 | `git status --porcelain \| grep -c discard-manifest` | measured | 39 untracked `.context/handovers/*.discard-manifest.yaml` files, dated `2026-09-16` through `2026-09-22` (one per session/compaction), none git-ignored (`git check-ignore -v` returns nothing), none referenced in rounds 1-3's reports | live, spans ≥6 days | structure/friction |
| E13 | `find .tasks -iname "*T-3420*"` + status grep | measured | `status: started-work`, `owner: agent` (task file, not the commit) — contradicts round 3 procasfit's handback characterisation ("independently completed") at the status-field level | live | structure |
| E14 | Same file, Agent ACs section | measured | All 5 Agent ACs read `[x]` (ticked), each with concrete verification evidence inline (bats 5/5, live `fw audit --section structure` PASS line quoted, fabric registration, vendor sync confirmed) | live | structure |
| E15 | `git show --stat 0943bfef0` | measured | Commit is real and landed: adds `lib/sidecar-audit.sh`, `agents/audit/audit.sh` WARN logic, fabric card, vendored copies, the task file itself (388 lines) — substantive, not a stub | live | structure |
| E16 | `python3 -c "yaml.safe_load(...)"` on `.gate-bypass-log.yaml` | measured, independent re-parse | 1212 entries — unchanged from round 3's count (round 2→3 was +1; round 3→4 is +0) | instant | structure |
| E17 | `.context/audits/2026-09-22.yaml` — fresh instance (mtime `10:19`, session-concurrent, later than round 3's `09:57` citation) | measured | `grep -i "hook.write.blind"` → no match | live | structure |
| E18 | `.context/audits/unit-suite/runs.log` tail | measured | No new cycle since round 3's own citation — both rounds read the same `2026-09-22T01:03-03:03Z DONE runner_exit=1 bats_rc=124 pytest_rc=124` entry; next cycle not yet due (`~2026-09-23T03:03Z`) | same window as round 3, no new data | usage/friction |
| E19 | `termlink channel snapshot seq:T-3411 --as-of <now-ms>` | measured, direct hub read | 13 rows total, rows [0]-[11] match rounds 1-3 exactly; row **[12]: `round=4 step=review worker=seq-t3411-r4-review status=dispatched result=docs/reports/SEQ-T3411/r4-review.md ts=2026-09-22T08:17:35Z`** — this worker's own dispatch, schema-correct, 4th consecutive round agreeing | live | structure |
| E20 | `fw sidecar inbox` (checked 2×: session start, pre-write) | measured | `no pending consults on sidecar:seq-t3411-r4-review` both times — 4th consecutive round with zero inbound organic consults on any of the (now 6, including this worker's own) review-worker addresses tried so far | live | usage |
| E21 | `bin/fw sidecar status` | measured, self-reported (same caveat as round 3's E15 — reads local ledger, not hub-side) | `messages total: 6 pending: 0` (unchanged from round 3 — no new traffic); inbox cursors show all 10 currently-registered sequence/ambient addresses, every `seq-t3411-*` address (now including `r4-review@0`) still at cursor `@0` | live | usage |
| E22 | `find .tasks -iname "*T-3417*"` + status grep | measured | `status: work-completed` — round 3 procasfit's close confirmed landed | live | structure |
| E23 | `find .tasks -iname "*T-3414*"` + status grep | measured | `status: work-completed` — round 3 procasfit's close confirmed landed; close condition for Δ9 met | live | structure |
| E24 | `grep -n "zzz-default" .agentic-framework/docs/generated/components/{lib-tasks,web-blueprints-tasks,web-templates-tasks}.md` | measured | All 3 vendored files still contain the stale reference — unchanged from round 3's E17; the Sovereign choice (Δ10) has not been made | live | structure |
| E25 | `find .tasks -iname "*T-3302*"` + status/owner grep | measured | `status: started-work`, `owner: human` — unchanged across all 4 rounds | live | structure |

## Non-use diagnosis — Δ4 (sidecar delivery observability), carried forward

| Reading | Evidence found this round |
|---|---|
| A BROKEN | No evidence of failed delivery attempts; nothing errors when exercised |
| B NEVER WIRED | Does not apply — mechanism proven reachable (rounds 1-3) |
| C UNDISCOVERABLE | Does not apply — documented in driver prompt, used correctly by this sequence |
| D UNMEASURED | **Still the best-fit reading**, but weaker than round 3's framing: T-3417 (the instrumentation) reached `work-completed` this cycle (E22) — the secondary signal round 3 predicted has now fired. The primary signal (an organic consult) still has not, across 4 consecutive rounds and 6 distinct addresses (E20, E21) |
| E NOT WANTED | No positive reason found; contradicted by T-3417's now-completed existence |

**Reading: D (unmeasured), instrumentation now complete, organic signal still absent** — an even sharper version of round 3's "trending toward resolution": the tool half of Δ4 is done; only the underlying question (does anything ever actually use this channel, from outside this sequence) remains open, and round 5 is the last chance this specific sequence gets to observe it directly.
