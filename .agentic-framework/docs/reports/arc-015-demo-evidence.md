# arc-015 — demo evidence: `fw init` reports what it found, on five real ecosystems

> Generated by `tests/demo/arc015_capture.sh`. Do not hand-edit — re-run the script.

| | |
|---|---|
| Captured at | `2026-08-10T19:58:08Z` |
| Framework commit | `bb003f458` |
| Arc | arc-015 `onboarding-shape-detection` |

## The claim under demonstration

arc-015's `headline_mechanic`, split into its three verifiable clauses:

1. the developer **watches it report what it actually found** in the directory
2. it **states the project shape it inferred** from that
3. it **seeds a task set they can start working immediately** — not an
   `owner: human` inception the agent is structurally forbidden to clear (F-10)

Each row below is a **fresh** directory containing real code of that ecosystem and
**no** manifest from the pre-T-2722 seven-entry allowlist, initialised by the real
`fw init` under `env -i`.

## Clause 1 and 2 — what the user sees

| ecosystem | clause 1: evidence line | clause 2: shape verdict |
|---|---|---|
| `dotnet` | `·  found 1 existing item(s): MyApp` | `✓  6 onboarding tasks (existing project mode)` |
| `ruby` | `·  found 2 existing item(s): Gemfile, app` | `✓  6 onboarding tasks (existing project mode)` |
| `php` | `·  found 2 existing item(s): app, composer.json` | `✓  6 onboarding tasks (existing project mode)` |
| `make` | `·  found 2 existing item(s): Makefile, main.c` | `✓  6 onboarding tasks (existing project mode)` |
| `gradle` | `·  found 2 existing item(s): build.gradle, src` | `✓  6 onboarding tasks (existing project mode)` |

## Clause 3 — is the seeded set actually startable?

| ecosystem | tasks seeded | `owner: human` |
|---|---|---|
| `dotnet` | 6 | 0 |
| `ruby` | 6 | 0 |
| `php` | 6 | 0 |
| `make` | 6 | 0 |
| `gradle` | 6 | 0 |

**Assertion: zero `owner: human` tasks across all five seeded sets.**

Stated explicitly rather than left to be inferred from a column of zeros, because
this is the entire F-10 failure: a greenfield-misclassified project was handed an
`owner: human` inception task, which the T-532 gate then used to block every other
edit — a first-run deadlock the agent cannot clear and the new user has no path to
diagnose.

## The guard

A demo shows the mechanic firing once. `tests/unit/init_project_shape_detection.bats` is what keeps it firing —
12 legs, covering the four F-10 misclassifiers plus the right-reason cases.

```
ok 10 negative control: the empty fixture is NOT reported as existing
ok 11 negative control: a project that IS recognised is NOT reported as greenfield
ok 12 negative control: _shape_of fails loudly when the mode line cannot be found
exit: 0
```

## What this artefact does NOT establish

- It does not show a **human** completing the onboarding set. That is arc-016's
  persona run and arc-017's gate; this is `fw init`'s output only.
- It does not cover ecosystems outside the five the headline mechanic names. The
  fix is enumeration-based, so others should follow — but "should" is not
  evidence, and this artefact contains none for them.
- It is a capture at one commit (`bb003f458`). It says nothing about any later one.
- **The consumers captured here are missing 7 enforcement hooks**, including
  `check-onboarding-gate` — see T-2911. That is not a defect in what this
  artefact demonstrates (`fw init`'s shape inference and seeding are unaffected),
  but it means these seeded projects are NOT running the gate that keeps the
  onboarding set clearable over time. arc-015's mechanic fires; arc-017's guard
  on it does not, in exactly the projects shown above.
