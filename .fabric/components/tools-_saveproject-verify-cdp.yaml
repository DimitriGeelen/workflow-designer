id: tools/_saveproject-verify-cdp.mjs
name: _saveproject-verify-cdp
type: script
subsystem: instruments
location: tools/_saveproject-verify-cdp.mjs
tags: [derived]

purpose: "_saveproject-verify-cdp.mjs — verify B3 (T-130): the editor \"Save to project\" button captures a thumbnail and POSTs to the B2 sidecar. Runs a sidecar on a free port against a TEMP repo+docroot (never the real repo), drives the editor in ISOLATED headless chromium (own --user-data-dir; never the shared browser, G-006), clicks Save, asserts a version with a thumbnail was written and the \"✓ Saved..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
