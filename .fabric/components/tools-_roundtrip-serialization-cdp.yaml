id: tools/_roundtrip-serialization-cdp.mjs
name: _roundtrip-serialization-cdp
type: script
subsystem: instruments
location: tools/_roundtrip-serialization-cdp.mjs
tags: [derived]

purpose: "_roundtrip-serialization-cdp.mjs — close G-002: prove the editor↔bridge aef: serialization seam is a SEMANTIC FIXED POINT, exercised through the REAL editor runtime (not a proxy).  The 7 existing seam guards are static/text checks that each cover one aspect (meta-parity, field-coverage, structured-parity, extension-shape, namespace, mapping-conformance, forward-fixtures). None runs a true round..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls
  - target: tests/test_roundtrip_serialization.py
    type: calls
  - target: tools/_t591-roundtrip-teeth.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
