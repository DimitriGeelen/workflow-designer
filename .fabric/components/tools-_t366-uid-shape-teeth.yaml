id: tools/_t366-uid-shape-teeth.py
name: _t366-uid-shape-teeth
type: script
subsystem: instruments
location: tools/_t366-uid-shape-teeth.py
tags: [derived]

purpose: "_t366-uid-shape-teeth.py — prove the uid-shape probe can SEE a shape validator. `_t366-uid-shape-agnostic.mjs` reports NO SHAPE CONSTRAINT, which is exactly what a probe blind to shape constraints also reports. This makes one exist. Two runs, neither touching src/ (the probe takes T366_SRC): control the real source -> expect rc=0, \"NO SHAPE CONSTRAINT\" teeth a copy whose import path coerces ->..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/_t366-uid-shape-agnostic.mjs
    type: calls

depended_by:
  - target: tools/_t366-uid-shape-agnostic.mjs
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
