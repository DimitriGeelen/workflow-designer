id: tools/_t347-accepted-element-content-cdp.mjs
name: _t347-accepted-element-content-cdp
type: script
subsystem: instruments
location: tools/_t347-accepted-element-content-cdp.mjs
tags: [derived]

purpose: "_t347-accepted-element-content-cdp.mjs — does content INSIDE an element the importer accepts survive the round trip?  WHY THIS EXISTS SEPARATELY FROM _t356. That instrument asks \"does a third-party document survive?\" and counts structure — nodes, flows, lanes, DI. T-347 asks a narrower question: when the importer ACCEPTS an element, does it keep what is inside it? An importer can score perfectl..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
