id: tests/test_t293_endpoint_reach.py
name: test_t293_endpoint_reach
type: script
subsystem: tests
location: tests/test_t293_endpoint_reach.py
tags: [derived]

purpose: "test_t293_endpoint_reach — standing endpoint-reconnect reachability leg (T-293, G-003 class), arc: designer-authoring-surface. The real work is `tools/_t293-endpoint-reach-cdp.mjs`, which drives the REAL editor in an isolated headless chromium with real CDP mouse input. It guards: * canvas layer order (#g-badges < #g-edges < #g-nodes < #g-badges-top < #g-handles < #g-preview) — T-286 badge laye..."

depends_on:
  - target: tools/_t293-endpoint-reach-cdp.mjs
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
