id: tools/_t479-endpoint-roundtrip-cdp.mjs
name: _t479-endpoint-roundtrip-cdp
type: script
subsystem: instruments
location: tools/_t479-endpoint-roundtrip-cdp.mjs
tags: [derived]

purpose: "_t479-endpoint-roundtrip-cdp.mjs — does <aef:endpoint> survive the editor round trip?  WHY this probe exists rather than a reading of the code: T-477 found the frozen two-party standard files `aef:endpoint` under the PRESENTATIONAL class (\"derived, never authoritative\"; \"a change alone MUST be a no-op for the task graph\") while it actually carries the executable command a task node runs. That i..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
