id: tools/_cdp-attach.mjs
name: _cdp-attach
type: script
subsystem: instruments
location: tools/_cdp-attach.mjs
tags: [derived]

purpose: "T-604 — one definition of \"attach to the page target\", shared by every CDP driver.  WHY THIS EXISTS. Chromium writes DevToolsActivePort when the BROWSER is listening. That is not the same instant the PAGE target is registered in /json. Every driver in tools/ had its own hand-copied spawn block that read the target list ONCE and dereferenced it with no retry:  cl = cdp(tg.find(t => t.type === 'p..."

depends_on:
  []

depended_by:
  - target: tools/_t604-cdp-attach-race.mjs
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
