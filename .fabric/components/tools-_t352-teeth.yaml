id: tools/_t352-teeth.sh
name: _t352-teeth
type: script
subsystem: instruments
location: tools/_t352-teeth.sh
tags: [derived]

purpose: "_t352-teeth.sh — prove each check in _t352-p011-errexit-probe.sh CAN fail, and fails for its OWN stated reason. Every leg requires a SPECIFIC substring: a leg that accepts any non-zero exit banks syntax errors as evidence (T-338 (d), T-343 (d), T-348 (c), T-350)."

depends_on:
  - target: tools/_t352-p011-errexit-probe.sh
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
