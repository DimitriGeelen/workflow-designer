id: tools/rail-sweep.py
name: rail-sweep
type: script
subsystem: instruments
location: tools/rail-sweep.py
tags: [derived]

purpose: "rail-sweep.py — enumerate every rail carrying MY fingerprint, and refuse to report all-clear from a source that cannot tell \"nothing unread\" from \"nothing tracked\". T-360. Prevention for G-022. WHY THIS EXISTS --------------- For ~24 days I reported \"AEF is silent\". That sentence was a one-topic measurement wearing the clothes of a fact about a peer. The natural check -- `termlink agent inbox`..."

depends_on:
  []

depended_by:
  - target: tools/_t360-rail-sweep-teeth.py
    type: calls
  - target: tools/_t379-rendered-as-wire-sweep.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
