id: tools/_selection-align-verify-cdp.mjs
name: _selection-align-verify-cdp
type: script
subsystem: instruments
location: tools/_selection-align-verify-cdp.mjs
tags: [derived]

purpose: "_selection-align-verify-cdp.mjs — verify T-134: selection-scoped Align / Distribute. Serves the editor from a TEMP docroot, drives it in ISOLATED headless chromium (own --user-data-dir; G-006). Multi-selects 3 nodes, asserts the Align/Distribute panel renders, then for each op perturbs the 3 nodes to distinct coords, clicks the button, and asserts the selection aligned/distributed (regardless o..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
