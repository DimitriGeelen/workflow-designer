id: scripts/announce-release.sh
name: announce-release
type: script
subsystem: scripts
location: scripts/announce-release.sh
tags: [derived]

purpose: "announce-release.sh — publish the current release identity to the AEF rail as ONE cv-indexed envelope, so AEF can answer \"am I current?\" with a live read."

depends_on:
  []

depended_by:
  - target: scripts/release-designer.sh
    type: calls
  - target: tools/_t389-release-envelope.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
