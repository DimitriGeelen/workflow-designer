id: tests/test_rule_dialect_axis.py
name: test_rule_dialect_axis
type: script
subsystem: ewcr-arc-0
location: tests/test_rule_dialect_axis.py
tags: [ewcr, arc-0, arc:ewcr-governed-delivery]

purpose: "T-325 (T-309 IW-1b) — every validator rule is classified UNIVERSAL / DIALECT-RELATIVE / PRESENTATIONAL, and the classification is DERIVED from the frozen standard rather than asserted. WHY THIS EXISTS --------------- `W-XML-GW-AMBIGUOUS` fires on 47 of AEF's 48 live gateways and on 0 of ours. It is not measuring gateway correctness; it is measuring which toolchain wrote the file. A rule like th..."

depends_on:
  - target: docs/standards/aef-bpmn-mapping-v1.md
    type: reads
  - target: tests/test_rule_form_parity.py
    type: calls
  - target: tools/validate-workflow.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-671

# Edges on this card are DERIVED, not asserted: tools/_t671-arc0-edge-derive.py
# extracts them from the referring file's own non-comment bytes. Regenerate with
#   python3 tools/_t671-arc0-edge-derive.py > /tmp/.e.tsv \
#     && python3 tools/_t671-arc0-card-gen.py /tmp/.e.tsv
