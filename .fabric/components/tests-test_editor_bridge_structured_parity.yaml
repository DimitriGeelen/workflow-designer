id: tests/test_editor_bridge_structured_parity.py
name: test_editor_bridge_structured_parity
type: script
subsystem: ewcr-arc-0
location: tests/test_editor_bridge_structured_parity.py
tags: [ewcr, arc-0, arc:ewcr-governed-delivery]

purpose: "test_editor_bridge_structured_parity — the editor and the bridge handle the SAME set of structured aef keys, on both the import and export sides (T-063). Fifth sibling in the editor↔bridge seam-guard family (namespace: T-044; shape: T-053; coverage: T-059; meta-parity: T-060; structured: this). T-063 gave the five structured-valued aef keys — emits, compensates (list) and aggregation, multiInst..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/yaml-to-bpmn.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-671

# Edges on this card are DERIVED, not asserted: tools/_t671-arc0-edge-derive.py
# extracts them from the referring file's own non-comment bytes. Regenerate with
#   python3 tools/_t671-arc0-edge-derive.py > /tmp/.e.tsv \
#     && python3 tools/_t671-arc0-card-gen.py /tmp/.e.tsv
