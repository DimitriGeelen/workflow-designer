id: tools/_typed-events-cdp.mjs
name: _typed-events-cdp
type: script
subsystem: instruments
location: tools/_typed-events-cdp.mjs
tags: [derived]

purpose: "_typed-events-cdp.mjs — T-204 Slice 1 correctness guard for typed intermediate events (error / timer / message). The round-trip harness (_roundtrip-serialization-cdp.mjs) proves parse→build is a SELF-CONSISTENT fixed point; it cannot prove CORRECTNESS (a consistently-wrong mapping — e.g. the typed event decoding back to linkEventCatch — would still be a fixed point). This harness asserts the ac..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tests/fixtures/aef-bpmn/typed-events.bpmn
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  - target: tests/test_typed_events.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
