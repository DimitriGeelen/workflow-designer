id: tools/_t423-additive-export-guard.py
name: _t423-additive-export-guard
type: script
subsystem: instruments
location: tools/_t423-additive-export-guard.py
tags: [derived]

purpose: "_t423-additive-export-guard.py — export ADDS DI and changes nothing else. Pairs each source corpus map with its export and requires that the two are identical on every element outside the DI namespaces: same tags, same attributes, same document order. Additions are permitted only in `bpmndi`/`dc`/`di`, plus two named provenance attributes on the root. WHAT THIS REPLACES, AND WHY THE AC IT SATIS..."

depends_on:
  []

depended_by:
  - target: tools/_t423-additive-export-cdp.mjs
    type: calls
  - target: tools/_t423-additive-export-teeth.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
