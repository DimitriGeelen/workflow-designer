id: tests/test_t125_lane_compaction.py
name: test_t125_lane_compaction
type: script
subsystem: tests
location: tests/test_t125_lane_compaction.py
tags: [derived]

purpose: "test_t125_lane_compaction — vertical lane-compaction regression wrapper (T-125), arc: designer-authoring-surface. The real work is `tools/_t125-lane-compaction-cdp.mjs`, which drives the REAL editor in isolated headless chromium over the full rendered corpus (24 maps) and asserts the compaction rule extracted from the T-122 operator correction pairs: * vertical-only — compactLanesFit() never to..."

depends_on:
  - target: tools/_t125-lane-compaction-cdp.mjs
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
