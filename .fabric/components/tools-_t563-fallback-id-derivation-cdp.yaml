id: tools/_t563-fallback-id-derivation-cdp.mjs
name: _t563-fallback-id-derivation-cdp
type: script
subsystem: instruments
location: tools/_t563-fallback-id-derivation-cdp.mjs
tags: [derived]

purpose: "The whole corpus, not only the fallback population — leg 4 needs the documents that DO carry the element, and the split is measured here rather than hard-coded so the probe reports the truth if the corpus moves."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
