id: tests/test_editor_behavior.py
name: test_editor_behavior
type: script
subsystem: tests
location: tests/test_editor_behavior.py
tags: [derived]

purpose: "test_editor_behavior — G-010 standing editor behavior suite wrapper (T-238), arc: designer-authoring-surface. The real work is `tools/_editor-behavior-verify-cdp.mjs`, which drives the REAL editor in an isolated headless chromium against a hermetic throwaway gallery sidecar (temp --repo + temp docroot — structurally incapable of touching the real registry or versions store). It encodes the two..."

depends_on:
  - target: tools/_editor-behavior-verify-cdp.mjs
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
