id: tools/_t358-lane-provenance-cdp.mjs
name: _t358-lane-provenance-cdp
type: script
subsystem: instruments
location: tools/_t358-lane-provenance-cdp.mjs
tags: [derived]

purpose: "Expectation table. `lanesOut` is what state should hold AFTER parse: the control keeps its own 2; every defaulted case gets our 3-lane skeleton."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tests/fixtures/lane-provenance/authored-lanes.bpmn
    type: reads
  - target: tests/fixtures/lane-provenance/empty-laneset.bpmn
    type: reads
  - target: tests/fixtures/lane-provenance/later-laneset-ignored.bpmn
    type: reads
  - target: tests/fixtures/lane-provenance/no-laneset.bpmn
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls
  - target: tools/_t358-teeth.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
