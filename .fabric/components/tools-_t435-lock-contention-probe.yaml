id: tools/_t435-lock-contention-probe.sh
name: _t435-lock-contention-probe
type: script
subsystem: instruments
location: tools/_t435-lock-contention-probe.sh
tags: [derived]

purpose: "_t435-lock-contention-probe.sh — does the push gate allow a push when the audit could not run?"

depends_on:
  []

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
