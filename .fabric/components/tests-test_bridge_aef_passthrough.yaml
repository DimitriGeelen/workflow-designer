id: tests/test_bridge_aef_passthrough.py
name: test_bridge_aef_passthrough
type: script
subsystem: ewcr-arc-0
location: tests/test_bridge_aef_passthrough.py
tags: [ewcr, arc-0, arc:ewcr-governed-delivery]

purpose: "test_bridge_aef_passthrough — pin the aef.x-* extension channel and the loud-drop contract on the YAML->BPMN bridge (T-061, closes FC-13). FC-13 (verified in T-058): the bridge emitted only a hardcoded set of aef.* keys and silently dropped every other one, so the aef: namespace the editor advertises as \"free-form passthrough\" was a closed, silent whitelist. Root of the T-059 / T-060 coverage b..."

depends_on:
  - target: tools/yaml-to-bpmn.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-671

# Edges on this card are DERIVED, not asserted: tools/_t671-arc0-edge-derive.py
# extracts them from the referring file's own non-comment bytes. Regenerate with
#   python3 tools/_t671-arc0-edge-derive.py > /tmp/.e.tsv \
#     && python3 tools/_t671-arc0-card-gen.py /tmp/.e.tsv
