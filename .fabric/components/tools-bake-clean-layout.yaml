id: tools/bake-clean-layout.py
name: bake-clean-layout
type: script
subsystem: instruments
location: tools/bake-clean-layout.py
tags: [derived]

purpose: "bake-clean-layout — bake the editor's Clean layout into the corpus (T-101). The rendered corpus (examples/aef-processes/rendered/*.bpmn) is machine-generated by tools/yaml-to-bpmn.py, which copies each node's x/y and each lane's height straight from the *.workflow.yaml source. Nobody hand-placed those nodes, so the editor's Tidy/Clean standard finds most of them untidy (T-100 messiness). This t..."

depends_on:
  - target: tools/_clean-layout-cdp.mjs
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls
  - target: tools/_t448-drift-classification-teeth.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
