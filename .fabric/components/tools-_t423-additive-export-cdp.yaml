id: tools/_t423-additive-export-cdp.mjs
name: _t423-additive-export-cdp
type: script
subsystem: instruments
location: tools/_t423-additive-export-cdp.mjs
tags: [derived]

purpose: "Written repo-relative, not as join(HERE, '<name>'), and that is not a style preference. tools/_t451-unwired-guard-census.py decides reachability by textual reference to `tools/<name>` in an executable position, so a runtime-composed path makes a guard that is genuinely invoked here report as UNWIRED — its own LIMIT paragraph names this false positive and says it cannot be closed from outside. T..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/_t423-additive-export-guard.py
    type: calls
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
