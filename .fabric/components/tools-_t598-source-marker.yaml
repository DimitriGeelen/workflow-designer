id: tools/_t598-source-marker.py
name: _t598-source-marker
type: script
subsystem: instruments
location: tools/_t598-source-marker.py
tags: [derived]

purpose: "T-598 — the source-end direction cue must exist, point forward, and stay smaller. The load-bearing assertion is orient=\"auto\". The destination markers use \"auto-start-reverse\", which is correct for marker-end but would make a marker-start point BACK at its own source — the exact opposite of the flow cue this is for. A future edit that \"tidies\" all four markers to one orientation would silently..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
