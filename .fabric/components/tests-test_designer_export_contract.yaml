id: tests/test_designer_export_contract.py
name: test_designer_export_contract
type: script
subsystem: ewcr-arc-0
location: tests/test_designer_export_contract.py
tags: [ewcr, arc-0, arc:ewcr-governed-delivery]

purpose: "T-202 (arc: designer-authoring-surface, post-GO on T-201 write-out): src-served guard that the designer's ``.bpmn`` EXPORT carries the write-out *content contract* that AEF's ``fw bpmn promote`` → ``fw task create`` (T-2541) consumes. The write-out seam resolved to **manifest-as-seam** (T-201 §3a): 832 owns the content, AEF owns the gated write. AEF's promote derives each emitted task's ``owner..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-671

# Edges on this card are DERIVED, not asserted: tools/_t671-arc0-edge-derive.py
# extracts them from the referring file's own non-comment bytes. Regenerate with
#   python3 tools/_t671-arc0-edge-derive.py > /tmp/.e.tsv \
#     && python3 tools/_t671-arc0-card-gen.py /tmp/.e.tsv
