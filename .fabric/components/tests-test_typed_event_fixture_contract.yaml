id: tests/test_typed_event_fixture_contract.py
name: test_typed_event_fixture_contract
type: script
subsystem: ewcr-arc-0
location: tests/test_typed_event_fixture_contract.py
tags: [ewcr, arc-0, arc:ewcr-governed-delivery]

purpose: "Typed-event fixture contract (T-212) — the 832-authored canonical typed-event fixtures for the designer↔AEF typed-event cross-validation seam (rail offsets 88/89; AEF ref T-2552 aef:eventDef detector), arc: designer-authoring-surface. WHY a sha-pin contract beside test_typed_events.py: that harness drives the REAL editor runtime (parseBpmnXml → buildBpmnXml) and asserts decode/encode CORRECTNES..."

depends_on:
  - target: tests/fixtures/aef-bpmn/typed-events.bpmn
    type: reads

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls
  - target: tests/test_harness_emitter_fidelity.py
    type: calls

last_verified: 2026-09-03
created_by: T-671

# Edges on this card are DERIVED, not asserted: tools/_t671-arc0-edge-derive.py
# extracts them from the referring file's own non-comment bytes. Regenerate with
#   python3 tools/_t671-arc0-edge-derive.py > /tmp/.e.tsv \
#     && python3 tools/_t671-arc0-card-gen.py /tmp/.e.tsv
