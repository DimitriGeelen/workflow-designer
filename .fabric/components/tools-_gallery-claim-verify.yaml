id: tools/_gallery-claim-verify.py
name: _gallery-claim-verify
type: script
subsystem: instruments
location: tools/_gallery-claim-verify.py
tags: [derived]

purpose: "_gallery-claim-verify.py — test for the T-228/S4a ui-claim path. Proves the 'create from pending ref' claim mechanics that gallery-serve.py applies on /api/save: when a saved map's OWN uuid matches a pending ghost, the server records a claim ({uuid,project,ts,via:\"ui\"}) in .context/designer/registry.yaml and drops the ghost — every referrer then resolves (its workflowRef now matches a live map..."

depends_on:
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
