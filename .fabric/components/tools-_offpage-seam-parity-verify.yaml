id: tools/_offpage-seam-parity-verify.py
name: _offpage-seam-parity-verify
type: script
subsystem: instruments
location: tools/_offpage-seam-parity-verify.py
tags: [derived]

purpose: "_offpage-seam-parity-verify.py — T-232 / S5a off-page seam parity guard. The PL-005/PL-030 drift counter for the off-page connector seam: a STATIC, pure-Python key-set-equality guard (PD-015) anchored on the SHARED byte-fixture that 832 AND AEF both pin — tests/fixtures/aef-bpmn/offpage-seam.bpmn (T-219, pair-draft #3). It turns S2's one-time round-trip check into a STANDING guard that runs eve..."

depends_on:
  - target: tests/fixtures/aef-bpmn/offpage-seam.bpmn
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
