id: tools/_t418-capture-attribution.sh
name: _t418-capture-attribution
type: script
subsystem: instruments
location: tools/_t418-capture-attribution.sh
tags: [derived]

purpose: "_t418-capture-attribution.sh — project a topic's envelopes down to the four fields attribution actually needs, and write NOTHING else to disk."

depends_on:
  []

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
