id: tools/_norec-verify.py
name: _norec-verify
type: script
subsystem: instruments
location: tools/_norec-verify.py
tags: [derived]

purpose: "_norec-verify.py — T-236 review-queue handoff guard. Exit 1 if any task with UNCHECKED Human ACs lacks a parseable `## Recommendation` verdict (GO/NO-GO/DEFER) — the NO-REC state that silently stalls the operator's approvals queue (\"agent thinks it handed over, operator sees nothing actionable\"). HTML comments are stripped first, mirroring the Watchtower queue's parser, so template example chec..."

depends_on:
  []

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
