id: tools/_t539-gap-closure-gauge-conformance.py
name: _t539-gap-closure-gauge-conformance
type: script
subsystem: instruments
location: tools/_t539-gap-closure-gauge-conformance.py
tags: [derived]

purpose: "T-539 teeth — every watching gap's closure gauge must return a verdict the reader accepts. A gap's `closure_check_command:` is the mechanical half of its closure condition. The audit already checks the PROSE half (`check_gap_triggers`, T-382, audit.sh:2175 — it verifies `decision_trigger` is present and sits under the key `fw gaps` renders). Nothing checked that the COMMAND produces something t..."

depends_on:
  []

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
