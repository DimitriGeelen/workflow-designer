id: tools/_t364-tie-guard-teeth.py
name: _t364-tie-guard-teeth
type: script
subsystem: instruments
location: tools/_t364-tie-guard-teeth.py
tags: [derived]

purpose: "_t364-tie-guard-teeth.py — prove the T-364 regression guard can go RED. `_t364-tie-permutes-ids.mjs` changed polarity when repair (a) landed. It used to exit 0 when the DEFECT was present; it now exits 0 when the REPAIR HOLDS and 1 on regression. That new failure branch has never fired, and a guard whose red is unproven is a guard that reports green for both \"the repair holds\" and \"the check si..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/_t364-tie-permutes-ids.mjs
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
