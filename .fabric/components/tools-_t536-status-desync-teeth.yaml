id: tools/_t536-status-desync-teeth.py
name: _t536-status-desync-teeth
type: script
subsystem: instruments
location: tools/_t536-status-desync-teeth.py
tags: [derived]

purpose: "T-536 teeth — CTL-028 must still detect a task whose location and status disagree. Drives the REAL audit binary (vendored .agentic-framework/agents/audit/audit.sh) through the TASKS_DIR seam against a synthetic task tree. The real .tasks/ is neither read nor written. WHY THIS IS DRIVEN AGAINST A SYNTHETIC TREE, NOT THE REAL ONE T-536 cleaned the four real offenders, so a guard asserting \"the re..."

depends_on:
  []

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
