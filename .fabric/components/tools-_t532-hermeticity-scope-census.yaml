id: tools/_t532-hermeticity-scope-census.py
name: _t532-hermeticity-scope-census
type: script
subsystem: instruments
location: tools/_t532-hermeticity-scope-census.py
tags: [derived]

purpose: "T-532 — census of hermeticity assertions, classified by SCOPE. THE QUESTION: a probe that checks \"did I leave the tree as I found it\" is doing the right thing. It becomes defective when the snapshot is scoped to the WHOLE REPOSITORY instead of to the paths its own subject writes, because then any other writer — cron, a handover commit, a concurrent agent — reddens it. Measured under T-527: one..."

depends_on:
  []

depended_by:
  - target: tests/run-bridge-tests.sh
    type: calls
  - target: tools/_t558-hermeticity-census-teeth.py
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
