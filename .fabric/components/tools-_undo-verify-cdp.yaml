id: tools/_undo-verify-cdp.mjs
name: _undo-verify-cdp
type: script
subsystem: instruments
location: tools/_undo-verify-cdp.mjs
tags: [derived]

purpose: "_undo-verify-cdp.mjs — verify B5 (T-132): general undo/redo history stack. Serves the editor from a TEMP docroot via gallery-serve.py on a free port; drives it in ISOLATED headless chromium (own --user-data-dir; never the shared browser, G-006). Exercises every wired mutation site through its real entry point and asserts undo restores / redo re-applies: 1. functions present (partial-not-broken..."

depends_on:
  - target: src/aef-workflow-designer.html
    type: reads
  - target: tools/gallery-serve.py
    type: calls

depended_by:
  []

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
