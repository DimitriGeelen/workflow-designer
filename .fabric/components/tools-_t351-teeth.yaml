id: tools/_t351-teeth.sh
name: _t351-teeth
type: script
subsystem: instruments
location: tools/_t351-teeth.sh
tags: [derived]

purpose: "_t351-teeth.sh — prove each check in _t351-shutdown-probe.sh CAN fail, and fails for its OWN stated reason. Every leg requires a SPECIFIC substring: a leg that accepts any non-zero exit banks syntax errors as evidence (T-338 (d), T-343 (d), T-348 (c), and it cost two false reds on T-350)."

depends_on:
  - target: tools/_t351-shutdown-probe.sh
    type: calls
  - target: tools/serve-gallery.sh
    type: calls

depended_by:
  - target: tools/_t509-instrument-sweep.sh
    type: calls

last_verified: 2026-09-03
created_by: T-673

# Purpose is SOURCED from this file's own header; edges are DERIVED from
# non-comment bytes. Neither is asserted. Regenerate (idempotent) with:
#   python3 tools/_t673-fabric-cards.py
