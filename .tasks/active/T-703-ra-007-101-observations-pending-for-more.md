---
id: T-703
name: "RA-007: 101 observations pending for more than 7 days"
description: >
  Audit WARN cycle 1 2026-09-16: stale observations in .context/inbox.yaml. Sibling
  of RA-006 - same register, different threshold.

status: started-work
workflow_type: build
owner: human
horizon: now
tags: [arc-003, audit-remediation, RA-007]
components: []
related_tasks: []
arc_id: arc-003
# arc_id:                         # T-1849: optional — slug (e.g. "arc-grooming") OR arc-NNN (e.g. "arc-005")
#                                 # When set, must resolve to .context/arcs/<id>.yaml; PreToolUse hook
#                                 # (check-arc-id) blocks save under agent control if it doesn't resolve.
#                                 # Empty/missing → unassigned (allowed). See CLAUDE.md §Task System.
created: 2026-09-16T13:25:11Z
last_update: 2026-09-20T09:09:45Z
date_finished:
# revisit_at: YYYY-MM-DD          # T-1451: set on DEFER decisions to enable G-053 daily revisit scan
# revisit_evidence_needed:        # T-1451: one-line description of what evidence makes the revisit actionable
# ── BVP scoring fields (T-1918, arc-006). See docs/reports/T-1915-bvp-inception.md for semantics. ──
# bvp_scores:                     # confirmed per-driver scores 0-5, set by `fw bvp confirm` (T-1924).
#                                 # Sovereignty boundary — only set after human or agent confirmation.
#                                 # Shape: {D1: <int 0-5>, D2: <int 0-5>, D3: <int 0-5>, D4: <int 0-5>, [<free-driver-id>: <int>]...}
# bvp_scores_proposed:            # estimator-proposed scores (T-1922 worker). Persists when ≥2 delta
#                                 # from bvp_scores: on any driver (M3 v2-delta). Shape: list of timestamped entries.
# cost_estimate:                  # F8 composite: 0.6×blast_radius + 0.3×tier + 0.1×effort.
#                                 # Q2 fallback: T-shirt S/M/L/XL mapped to 2/4/6/8 when blast_radius is not yet computable.
bvp_scores_proposed:
  - ts: '2026-09-16T13:30:33Z'
    estimator: bvp-estimator-v1-heuristic
    scores:
      D1: 4
      D2: 4
      D3: 2
      D4: 2
      F-RECALL: 2
      F2: 0
      F4: 0
      F3: 0
      F1: 1
    rationale: D1=4 (body:structural-gate); D2=4 (body:fw-audit-or-doctor); D3=2
      (body:default-change); D4=2 (body:env-class-handled); F-RECALL=2 
      (body:lightly-promoted); F2=0 (no-signal); F4=0 (no-signal); F3=0 
      (no-signal); F1=1 (prose:process-enablement-incidental)
    rubric_sha: e4a00f38e801
cost_estimate_proposed:
  - ts: '2026-09-16T13:30:50Z'
    estimator: bvp-estimator-v1-heuristic
    cost_estimate:
      tier: 2
      effort: 7
      blast_radius: 1
    rationale: blast_radius=1 (paths:.context/inbox.yaml); tier=2 (no-signal); 
      effort=7 (no-signal)
    rubric_sha: e4a00f38e801
---

# T-703: RA-007: 101 observations pending for more than 7 days

## Context

**Finding RA-007** - cycle 1, 2026-09-16. Source: fw audit. Severity: WARN.

Cycle-1 baseline for regression detection: audit Pass 166 / Warn 31 / Fail 1; doctor 3 warn / 0 fail.

**Verbatim tool output:**

```
[WARN] 101 observation(s) pending for >7 days
       Evidence: Stale observations in .context/inbox.yaml
       Mitigation: Run: fw note triage - promote or dismiss stale items
```

**The invariant that is not held:**

Same register as RA-006, different threshold: age rather than severity. The invariant not held is that the inbox is a queue, not an archive. At 101 items over 7 days old the register has stopped being a work surface and become a log. Kept separate from RA-006 per the one-finding-one-task rule; the shared root cause is that no verb drains this register under agent authority.

**Root-cause siblings:** T-702 (RA-006)

**Operator action / sovereignty boundary:**
Same boundary as RA-006. Note that this arc's own 35 tasks do not enter this register, so RA-007's count is unaffected by this run's task creation.

**Verification is the next cycle's re-run of the originating check, not this task's own assertion that it is fixed.**

## Acceptance Criteria

### Agent
<!-- Criteria the agent can verify (code, tests, commands). P-010 gates on these. -->
- [x] `fw audit --section observations` reports 0 observations pending over 7 days, or the residue is enumerated with per-item reasons — **enumerated, not drained.** `docs/reports/T-703-inbox-residue.md` (generated by `tools/_t703-inbox-residue.py --write`) lists all 107 stale items with a reason class derived from the register: 82 carried-by-completed-task, 20 carried-by-active-task, 4 orphan (OBS-249, OBS-251, OBS-256, OBS-306), 1 dangling-task (OBS-309 → a peer task id). `--check` exits 0 only when every stale row carries a reason; the count (107) equals the audit's own `[WARN] 107 observation(s) pending for >7 days` measured 2026-09-20.
- [x] The structural question - whether any agent-authority drain path exists for this register - is answered in this task, not assumed — **answered: it exists and is not auditable.** Two closing verbs, both in the vendored `.agentic-framework/agents/observe/observe.sh`: `promote` (creates a task — work, not closure) and `dismiss` (`do_dismiss`, observe.sh:280-298: flips `status:` via sed, ECHOES `--reason` to stdout, persists nothing — no reason, no actor, no timestamp). `tools/_t703-inbox-residue.py --probe-dismiss` runs the real verb against a copy under a throwaway `PROJECT_ROOT`, hashes the real inbox before and after (byte-identical), and confirms: status flipped, reason NOT persisted, no `dismissed_by/at`. This was already on record as OBS-290 (2026-08-17, T-557) — itself 34 days stale in the residue it describes. So: an agent CAN drain the register mechanically, and under L-1699 ("dismissing must name where the content now lives") and producer-not-judge it MUST NOT, because the drain leaves no rationale to audit. Zero items were dismissed by this task. See `## Recommendation`.

### Human

- [ ] [REVIEW] Rule on the drain path for the 82 carried-by-completed-task items (the bulk of the residue)
  **Steps:**
  1. Open http://192.168.10.107:3013/review/T-703 and read `## Recommendation`
  2. Open `docs/reports/T-703-inbox-residue.md` — the reason census at the top is the whole picture; the per-item table is for spot checks
  3. Choose: **A** — fix the vendored dismiss verb first (persist reason/actor/timestamp, G-008 in-tree + upstream), THEN authorise agents to dismiss `carried-by-completed-task` items citing the episodic that carries the thread; **B** — operator dismisses in bulk via `fw note triage` and accepts that the register records no reason (the current verb's behaviour); **C** — leave the register as an archive: retire RA-006/RA-007 as audit findings (threshold or scope change in audit.sh) rather than draining
  **Expected:** a letter recorded on this task; under A, a one-bug task is filed for the verb (recommended — the fix is ~10 lines and OBS-290 has waited 34 days for it)
  **If not:** the 107 stays a WARN every audit cycle and RA-006/RA-007 re-file on the next remediation sweep


## Verification

# Shell commands that MUST pass before work-completed. One per line.
# Lines starting with # are comments (skipped). Empty lines ignored.
# The completion gate runs each command — if any exits non-zero, completion is blocked.
#
# Toolchain hint (L-291): if you edited *.vbproj/*.csproj/*.xaml add `dotnet build`;
# *.go → `go build ./...`; Cargo.toml → `cargo check`; tsconfig.json → `tsc --noEmit`;
# pom.xml → `mvn -q compile`. P-011 runs only what you write — broken builds slip
# past otherwise (origin: 003-NTB-ATC-Plugin T-077, broken WPF DLL on master 5 days).
#
# ⚠ ERREXIT WARNING (T-352) — READ BEFORE USING THE CAPTURE PATTERN BELOW.
# P-011 runs each command under `-o pipefail` but NOT under an effective `-e`.
# Measured, not assumed (tools/_t352-p011-errexit-probe.sh): the gate runs each line as
# `if ( … eval "$cmd" ); then` (update-task.sh:1018) and that subshell is the CONDITION
# of an `if`, which neutralises errexit inside it. pipefail survives; errexit does not.
# CONSEQUENCE: a line of the form `a; b` IS JUDGED ON `b` ALONE. `a`'s exit code is
# discarded, so a command that fails outright can still leave the line green.
#   Proven false green:
#     out=$(python3 tools/validate-workflow.py BROKEN.bpmn 2>&1); echo "$out" | grep -q "VALID"
#   -> PASSES on a document the validator exits 2 on and labels INVALID, because
#      `grep -q "VALID"` matches INVALID as a SUBSTRING. Two defects stacked.
# PREFER a single command whose own exit code is the verdict — then no context question
# arises. When you must chain, the LAST command has to be the one that can fail, and its
# pattern must not be matchable by the earlier command's FAILURE output.
# Note `set -e` re-issued inside the subshell does NOT fix this: the suppressed context is
# inherited and re-setting the option does not clear it. See T-352 for the remedy.
#
# Pipefail/SIGPIPE hint (L-387): `cmd | grep -q PATTERN` exits 141 (SIGPIPE) when grep
# matches and closes stdin while the upstream is still writing — verification then
# "fails" even though the pattern was present. The capture pattern below fixes THAT,
# and creates the errexit exposure described above; the file form fixes both:
#     cmd > /tmp/.out 2>&1 && grep -q "PATTERN" /tmp/.out     # PREFERRED: && not ;
#     out=$(cmd 2>&1); echo "$out" | grep -q "PATTERN"        # SIGPIPE-safe, errexit-blind
# Origin: L-387, captured 4× (T-1716, T-1838, T-1862, T-1863) before this hint.
#
# Single pipe only — no intermediate tail/awk/sed stages between capture and grep
# (T-2090): `echo "$out" | tail -3 | grep -q PAT` re-introduces the SIGPIPE risk
# the capture step closed off — the middle stage is what `grep -q` slams its
# stdin on. `echo "$out"` is small and immediate; grep scans the whole captured
# string anyway, so the tail-3 was cosmetic. Drop it: `echo "$out" | grep -q PAT`.
#
# Enforcement-baseline hint (L-398, T-1886): if you edited `.claude/settings.json`
# (added/removed/reorganised hooks), add `bin/fw enforcement baseline` to your
# Verification block. Otherwise the canonical hash diverges and `fw doctor`
# reports a FAIL ("Enforcement baseline CHANGED") that accumulates silently.
# Origin: T-1849/T-1730/T-1731 each added a legitimate hook without refreshing
# the baseline — FAIL sat for multiple sessions until T-1886 cleaned up.

# Parked: triage verdicts are the operator's. Verification is the next cycle's audit re-run.

# T-703 legs. The instrument reads the register with a YAML parse (exact), never grep.
python3 tools/_t703-inbox-residue.py --check
python3 tools/_t703-inbox-residue.py --probe-dismiss
test -f docs/reports/T-703-inbox-residue.md
# the enumeration is regenerable and every stale item is in it: row count == audit's count for the same day
test "$(grep -c '^| OBS-' docs/reports/T-703-inbox-residue.md)" -eq "$(python3 tools/_t703-inbox-residue.py | sed -n 's/^T-703 — inbox residue: \([0-9]*\) .*/\1/p')"
# (the probe hashes the real register before/after itself and exits 2 if it moved — no separate git-diff leg,
#  which would go red on any unrelated uncommitted `fw note` capture)

## RCA

<!-- REQUIRED for bug-class tasks (workflow_type=build with bug-tag, OR title matches
     fix/bug/rca/broken/crash/error/regression/fail/hotfix).
     Non-bug-class tasks may leave this section empty or remove it.

     For bug-class, fill in:
       **Symptom:** what was observed (the user-facing manifestation).
       **Root cause:** the specific structural/logical gap — not "the code was wrong".
       **Why structurally allowed:** what in the framework/code/tooling let this go undetected.
       **Prevention:** what catches the next instance (test/lint/gate/doc/learning) — distinct from the fix itself.

     The completion gate (T-1550, G-019) blocks --status work-completed when
     bug-class AND this section is empty/template-only. Use --skip-rca to bypass (logged).
-->

**Symptom:** 107 observations pending more than 7 days (audit WARN, cycle 1 said 101 — it grew by 6 while the finding sat as a task).

**Root cause:** the register has no auditable drain. `fw note dismiss --reason` discards the reason (observe.sh:280-298), so under L-1699 an agent cannot legitimately dismiss anything, and the operator has not been asked to. Pending is the only state an entry can honestly stay in.

**Why structurally allowed:** the dismiss verb was shipped with a `--reason` flag that is parsed and printed, which makes it LOOK recorded. OBS-290 (2026-08-17) named this precisely and has been pending 34 days — the register cannot surface a defect in its own drain, because that defect is what keeps the register from draining.

**Prevention:** `tools/_t703-inbox-residue.py --probe-dismiss` is a standing check that fails the day the verb starts persisting the reason (VERDICT line changes) — i.e. it will tell whoever fixes it that the fix landed, and it will tell anyone who assumes the reason is recorded that it is not. The residue enumeration replaces "107" with a reason census, so the next remediation sweep files against the census, not the number.


## Evolution

<!-- REQUIRED for arc-tagged build tasks (tags include arc:*). Captures how
     understanding evolved during build — what was learned that wasn't known at
     filing, what in the original plan no longer fits, what triggered pivots
     or new sub-tasks. Mandatory at slice boundaries (when applicable) and
     before --status work-completed.

     Origin: T-1717 grill Q4 — "the understanding of what we need and want
     evolves with the process of materialisation." Structural counter to §ACD:
     spec-vs-build divergence is logged as soon as it happens, not lost as
     folklore.

     Format (one entry per slice boundary or significant insight):
       ### YYYY-MM-DD — [topic]
       - **What changed:** [what we learned that we didn't know at filing]
       - **Plan impact:** [what in the plan no longer fits]
       - **Triggered:** [new sub-task / pivot / scope cut, with task ID if filed]

     The completion gate (T-1718) blocks --status work-completed when this
     section exists but is empty/template-only. Use --skip-evolution to bypass
     (logged Tier-2). Non-arc tasks may leave this empty.
-->

Filed as "drain the stale items". Landed as "the register cannot be drained honestly, and here is what it is made of". Two turns:

1. The AC2 question (does an agent-authority drain path exist?) turned out to have been answered on 2026-08-17 by OBS-290 — sitting unread in the very residue this task measures. Reading the register before reaching for the verb was the whole task.
2. Three instruments count the same "urgent pending" field and give 36 / 41 / 3 (audit yaml parse / observe.sh grep -c / handover.sh block split on `\n  - `, which fragments on bullet lists inside observation text). The handover's "3 urgent" that every session has been reading is 33 short. Not fixed here — one bug, one task — recorded under `--counters` and in `## Decisions`.


## Decisions

<!-- Record decisions ONLY when choosing between alternatives.
     Skip for tasks with no meaningful choices.
     Format:
     ### [date] — [topic]
     - **Chose:** [what was decided]
     - **Why:** [rationale]
     - **Rejected:** [alternatives and why not]
-->

### 2026-09-20 — enumerate, do not dismiss
- **Chose:** satisfy AC1 by enumeration with per-item reason classes, and dismiss zero observations.
- **Why:** the only closing verb drops its rationale; dismissing 82 `carried-by-completed-task` items would be a sweep whose reasons live nowhere — exactly what T-702's AC2 forbids and what L-1699 warns against. The reason census is checkable; a cleared counter is not.
- **Rejected:** (a) dismissing with the reason written into the task file instead of the register — the register would still read as reasonless, and a reader of the inbox has no pointer to the task; (b) fixing the vendored dismiss verb inside this task — one bug, one task, and the fix's shape (which fields, whether agent dismissals are permitted at all) is the operator's ruling above.

### 2026-09-20 — the handover urgent counter is a separate finding, not fixed here
- **Chose:** record handover.sh:382's 3-vs-36 undercount in the tool's `--counters` output and in this task; do not patch handover.sh under T-703.
- **Why:** the enumeration is this task's deliverable; patching a second vendored script under it would open a second structural change while the first (the dismiss ruling) is ungated.
- **Rejected:** filing it as a new observation — it would join the residue it describes. It goes in the handback as a candidate one-bug task.


## Recommendation

**Recommendation:** GO
**Rationale:** Option A. The drain path is mechanically open and evidentially closed: `dismiss` records nothing, so no dismissal by anyone — agent or operator — is auditable today. Fix the verb first (persist `dismissed_at`, `dismissed_by` from `$CLAUDECODE`/actor, `dismissed_reason`, and a `content_lives_in:` pointer per L-1699 — ~10 lines in the vendored script, G-008 in-tree + upstream), then authorise agent dismissal for the one class where the pointer is mechanical: `carried-by-completed-task` (82 of 107), citing `.context/episodic/<task>.yaml`. The 4 orphans (OBS-249 budget ladder — since re-worded by T-614, likely resolved; OBS-251 shared identity; OBS-256 bridge-suite scheduling; OBS-306 census inflation) and the 20 active-carried items stay with their tasks or the operator. Option B clears the number and keeps nothing; C changes what the audit measures instead of what the register is.
**Evidence:** `docs/reports/T-703-inbox-residue.md` (107 rows, census 82/20/4/1); `tools/_t703-inbox-residue.py --probe-dismiss` VERDICT CONFIRMED with real inbox hash unchanged; OBS-290 (2026-08-17) prior record of the same defect; observe.sh:280-298; audit 2026-09-20 `[WARN] 107 observation(s) pending for >7 days`.

## Decision

<!-- Filled at completion of inception tasks via:
     fw inception decide T-703 go|no-go|defer --rationale "..."

     For non-inception tasks this section is ignored. Kept in template
     so `fw inception decide` (lib/inception.sh) finds the anchor heading
     without auto-creating; T-1832 added auto-create as fallback for
     legacy tasks lacking this section. -->

## Updates

### 2026-09-16T13:25:11Z — task-created [task-create-agent]
- **Action:** Created task via task-create agent
- **Output:** /opt/832-Workflow-designer/.tasks/active/T-703-ra-007-101-observations-pending-for-more.md
- **Context:** Initial task creation

### 2026-09-20T09:03:56Z — status-update [task-update-agent]
- **Change:** status: captured → started-work
